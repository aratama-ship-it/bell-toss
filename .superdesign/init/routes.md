# Routes

This is a static, single-page application with no router.

| URL | Entry | Layout |
|---|---|---|
| `/` | `index.html` | Complete application shell in `index.html` |

The root page renders the stage simulator, transport/configuration controls, ring and warning summaries, editable piano roll, and computed performer timeline.
