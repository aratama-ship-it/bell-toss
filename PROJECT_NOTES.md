# 投鈴（とうれい）— ハンドベル・ジャグリング編成シミュレーター

作成: 2026-08-21（Claude Fable 5が設計・実装・検証）
更新: 2026-08-22 タイムラインナビゲーションUI実装（Claude Fable 5、詳細は後述の節）
場所: `apps/utility-app/torei-bell-toss/`（iCloud共有ワークスペース内）

## これは何か

空転軌道のハンドベル・ジャグリング演技（リングの内側にハンドベルを固定し、
投げて**キャッチした衝撃で音が鳴る**ことでメロディを奏でる）に着想を得た、
**非公式の自作シミュレーター**。メロディを入力すると、各演者が
「いつ・どのリングを・どちらの手で投げ、いつキャッチするか」を逆算して、
舞台アニメーション＋行動表として表示する。

参考動画: https://www.youtube.com/shorts/kVeFwA3Rp4M
（3人・リング＋ベル・スタンドに音違いのリング多数、という構成はこの動画で確認）

## 演技モデル（2026-08-21 本人確認済み）

- 1リング = 1音高。キャッチの衝撃（または手元で振る）で鳴る。
- 演者は手2本。予備リングの置き場は2種類:
  **脇に挟む**（速い持ち替え 0.7秒・**1人1本まで**）／
  **スタンドに置く**（遅い・既定4.0秒・移動込み。UIで調整可）。
  ※「スタンドと脇の両方がある」「スタンドは時間に余裕が欲しい(2倍)」「脇は1人1本」は本人指定。
- 滞空時間は投げの高さで調整可（**0.7〜1.4秒**）。高さは h = g*t^2/8 で決まり、
  1.4秒 = 手の高さから約2.4m上 = 床から約3.8m。**それ以上は天井に当たるので上限**
  （2026-08-21 本人指摘「一番高く投げているものは現実ではない高さ」を受けて 2.2秒から変更）。
- **スタンドの出し入れ中はジャグリングできない**（体ごと動くため両手とも塞がる。
  空中にリングがある間もスタンドへ行けない）。2026-08-21 本人指摘。
- 曲開始前の「準備」（負の時刻）に取得・投げをしてよい。
- 投げが間に合わない音は（設定ONなら）手元で振って鳴らし、警告を出す。

- **★リングを持っている手で別のリングをキャッチすると、持っている方にも衝撃が伝わって
  必ず両方鳴る**（2026-08-23 本人確認）。つまり「持ったままキャッチ」は保持キャッチ和音として
  意図した場合にのみ許され、それ以外で起きると楽譜にない音が鳴る。
  検証は `node tools/check_held_catch.mjs`（常に0件であること）。

## 使い方

ブラウザで `index.html` を開くだけ（ビルド不要・依存なし・全て素のJS）。
ローカルサーバー例: `python3 -m http.server 8934 --directory <このフォルダ>`
このMacでは `.claude/launch.json` に `torei-bell-toss` として登録済み。

- 上段の楽譜（ピアノロール）で音符を編集する。16分単位:
  **空白をクリック=追加／音符をドラッグ=移動（時間・音高とも）／その場でクリック=削除**。
  ドラッグ中は音高が変わるたびにベルが鳴り、離した時点で編成を再計算する。
- **「MIDI読み込み」**で手持ちの標準MIDIファイルを取り込める（`parseMidi()`）。
  最も音数の多いトラックをメロディとして採用し、テンポ・拍子も読む。
  表示範囲(C4〜C6)を外れる音はオクターブ単位で折り返し、その旨を通知に出す。
  注: 8分音符基準で書き出した曲（グリーンスリーブス等）を読み直すと、
  4分音符基準に換算されてBPM表示が半分になる。**実時間は変わらない**。
  読み込んだ曲には推奨設定（人数・滞空）が付かないので、必要なら手で合わせる。
- プリセット4曲（きらきら星・チューリップ・メリーさんのひつじ・よろこびの歌 = 全てPD）。
- 下段の行動表: 音符から左へ伸びる弧が「投げ→鳴るまで」の滞空。左手・右手の2レーン、
  レーンをまたぐ弧は「逆の手で受ける」。帯は 台→手 / 手→脇 などの持ち替え。
- 「MIDI書き出し」= SMF format 1（トラック: テンポ／メロディ全体／演者ごと）。
- 「行動表をコピー」= 練習用テキスト。

## 実装の要点（引き継ぎ用）

- `js/scheduler.js` が中核。**手の空きを busy区間の集合、保持リングを {ring,from,to} 区間で管理**。
  スカラー(freeAt)管理では「キャッチ予約より前の空き時間」が消えて破綻する（実装済みの教訓）。
- **`preassign()` が中核**: 曲頭で音高を演者へ配り、開演前にスタンドから手・脇へ移す。
  これを入れる前は「その場しのぎの割り当て」で曲中に無駄なスタンド往復が多発し破綻していた。
- 投げ手とキャッチ手を分離（投げた手は滞空中に次の作業ができる）。キャッチ手は
  他の演者でもよい＝**パッシング**（詳細は後述の節）。
  パスの受け手は「直後に自分の担当音を投げる予定の演者」を避ける（手を塞ぐと破綻するため）。
- 取得・持ち替えは基本ジャストインタイム配置。ただしスタンド取得は
  「早取り」「補充（台→手→脇）」の段取り探索あり（後述のスタンド持ち替え節）。
- キャッチ後、その音の次の使用時刻を先読みして リングを脇(>2.5秒先)/スタンド(遠い将来のみ) へ戻す。
- 同音の連打はリング複製（同音リング最大 = UI設定）で別演者に分担。
- greedy一発では品質が揺れるため、**コストジッター付き乱択リスタート20回**
  （xorshift32・シード固定で再現可能）から最良を採用。計算は全プリセットで100ms前後。

## ファイル構成

- `index.html` / `css/style.css` — UI。配色はベース紙白/メイン墨/アクセント真鍮。
  音高色はトーン・イン・トーン（彩度を揃えた多色）。
- `js/presets.js` — 「白紙から作る」＋演者名・音名(ドレミ)・音高色のユーティリティ
  （楽曲プリセット本体は `js/songs.js` に一本化。2026-08-22 整理）
- `js/scheduler.js` — 逆算スケジューラー（上記）
- `js/audio.js` — Web Audioハンドベル音源（加算合成＋打音ノイズ、セッションバスで停止管理）
- `js/midi.js` — SMF書き出し
- `js/pianoroll.js` / `js/timeline.js` / `js/stage.js` — 楽譜・行動表・舞台ビュー描画
- `js/navigator.js` — 全曲ナビゲーター＋小節ルーラー（2026-08-22 追加。下記参照）
- `js/main.js` — 全体制御・再生・編集・シーク・ズーム・ループ

## タイムラインナビゲーションUI（2026-08-22 実装）

`.superdesign/design-system.md` の仕様を実装。デザインの考え方は「舞台監督の見取り図」:
DAWの模倣ではなく、稽古の現場に実在する道具（小節番号・バミリ・開いたページ）の翻訳。

- **ナビゲーター帯（全体）**: 全曲を音高色の墨点で圧縮表示。いま見えている範囲だけ
  紙が明るく「開いて」おり、外側には紗がかかる。窓をつかんでパン、**窓の端（真鍮の
  つまみ）を引いてズーム**、窓の外をクリック/ドラッグでシーク。
  **振り・不可能の音は赤点**で打ち、全曲を見渡して破綻箇所が分かる。
- **ルーラー（小節番号の帯）**: なぞって再生位置（16分スナップ）。**option+ドラッグで
  ループ範囲**を直接指定。音符編集（ピアノロール）とは完全分離なので誤操作しない。
- **再生カーソルは常設**: 止めても消えず、その位置から再生を再開。カーソルが曲頭の
  ときだけ準備（-4秒〜）から見せる。シーク時は舞台ビューも即時にその瞬間へ。
- **ループ稽古**: 「この小節を反復」ボタン＝現在の小節、ブラケット（バミリ）を
  ドラッグで調整。**巻き戻しはrAFでなくタイマー駆動**——タブを裏に回して
  「音だけ聞きながら手を動かす」稽古でも途切れない（検証済み: 非表示タブで
  5.2秒間に2回の巻き戻しを確認）。
- **ズーム**: PPB 10〜140。−/＋ボタン、スライダー、⌘+ホイール（ピンチ）、窓の端。
  基準点（ポインタ/プレイヘッド）の拍を固定して伸縮。**上限は曲の長さから逆算**して
  Canvasの32767px制限を超えない（長いMIDIの描画崩れを予防）。
- **キーボード**: Space=再生/停止、←→=16分（Shiftで1小節）、Home=曲頭。
  中ボタンドラッグで楽譜パン。入力欄フォーカス中は無効。
- **既存バグ修正**: TOTAL_BEATSが64拍固定で、聖者の行進（91拍）等の後半が楽譜から
  はみ出て見えなかった → recompute()が曲の長さから自動算出するように。
  併せてBPM手入力上限を180→200へ（疾風187BPMがプリセットにあるため）。
- 実装時の注意: `TOREI.view.x(beat)` に座標系が集約されているため、ズームは
  PPBを変えるだけで全描画が追随する。ナビゲーターは `#nav-body`（伸縮する親）から
  幅を測る（canvas自体はsetupCanvasが幅を固定するため）。ループ音声の予約は
  ループ終端で打ち切り、`scheduleFrom` が終端時刻のタイマーで次の周を予約する。

## 行動表の再生位置ハイライト（2026-08-22 実装。design-system.md 項目8）

再生位置で「各演者・各手が今どの行動をしているか」を一目で見せる。演者×手の数だけ
（例: 3人なら6個）小さなDOM要素（`.action-hl`）を作り、毎フレームは`left/top/width/height`
だけ書き換える。**行動表Canvas（幅が数千〜1万px超）自体は毎フレーム再描画しない**——
これが課題として残っていた「全幅再描画を避ける」ことの実装解。

- `js/timeline.js` の `layoutAction(action, spb)` が、1アクション（投げ・キャッチ・
  振り・持ち替え）の見た目上の矩形をバンド内ローカル座標で返す。`draw()`（静的な
  全体描画）と `main.js` のハイライト更新が、この同じ関数を共有する。
- 保持キャッチ和音（held/new）は同じ(演者,手,時刻)に2件重なるため、
  `main.js` の `updateActionHighlight()` は該当アクション全てを集めて**矩形の和集合**を
  1つのハイライトとして表示する（実測: 高さ25pxの枠が held/new 両方のドットを囲む）。
- **実装直後に見つけた重大バグ**: `hlEls`（`{el, perf, hand}` のラッパー配列）を
  再構築する際、`for (const el of hlEls) el.remove()` としてしまい、`el` が
  ラッパーオブジェクトのため `el.remove is not a function` で**毎回の再計算
  （曲切り替え・音符編集・BPM変更など）が必ず例外で落ちていた**。
  `drawAll()` 内で `buildHighlightEls()` より後にある
  `updatePlayheadEl()`／`updateReadout()`／`updateZoomUI()`／ナビゲーター再描画も
  巻き添えで止まる実害があった（正しくは `item.el.remove()`）。
  → 教訓: 新しいDOM管理コードを足したら、**2回連続で状態変更**（例: 曲を2回切り替える）
    して確認する。1回目は配列が空なので問題が顕在化しない。

## ルーラーのタッチ当たり判定拡大（2026-08-22 実装。design-system.md 項目8）

小節番号の帯（ルーラー）は見た目22pxのままだが、指では細すぎて狙いにくい。
design-system.mdの「タッチ対象は見た目より細くても最低44 CSS pixels」を満たすため、
**見た目の高さは変えず、当たり判定だけ広げる透明レイヤー**を追加した。

- `#ruler-hit`（`index.html`、`#ruler`canvasの直後）: `position:absolute; top:0; height:44px`。
  ルーラー操作のイベント（`pointerdown/move/up`）は全てこの透明divが受ける
  （`js/navigator.js`の`wireRuler()`が対象を`#ruler`から切り替え）。左端は`#ruler`と
  揃っているので`ev.offsetX`の座標系はそのまま使い回せる。幅は`drawRuler()`実行のたび
  `#ruler`と同じ`V.width`へ同期する。
- **上ではなく下（楽譜側）へだけ拡張**: `#scroll-area`が`overflow-y:hidden`のため、
  `top`を負値にして上へはみ出す実装は途中でクリップされて機能しない
  （実装中に気づいた・上方向拡張は不可）。下方向だけなら`#scroll-area`自身の
  ボックス内に収まるため問題なく機能する。トレードオフとして、ピアノロール最高音行
  （C6、上端 約22px分）の一部がルーラー操作に取られる——タッチでその境界に近い操作は
  ルーラー優先という設計判断。
- `position:absolute`の要素はz-index未指定でも通常フローの`<canvas>`（ルーラー本体・
  ピアノロール）より常に上に描画される（CSSのスタッキング順の性質）ため、DOM上の
  挿入順（`#ruler`の直後）に関わらずこの重なり方が保証される。
- 検証: 実クリックで「ルーラー本体の直下22px（本来ならピアノロールに見える範囲）」を
  押してもノートが追加されずシークだけが起こること、それより下（本来のピアノロール）
  では通常通りノート追加が起こることの両方をring-summaryの変化で確認済み。

## design-system.md 未達項目の棚卸しと修正（2026-08-23）

実機（ローカルHTTP + ブラウザ）で design-system.md の全項目を突き合わせ、未達5件を直した。
**根の原因が「JSがCSSを上書きしていた」型だった**のが今回の学び。

### 直したもの

1. **読み込み・曲切替の直後に曲頭が画面外だった**（`js/main.js`）
   `loadPreset()` が `scrollLeft = 0` に戻していたが、準備ゾーンは曲によって6〜27拍と幅がある。
   聖者の行進で `x(0)=952px`・表示幅515pxのため、**起動直後は空の準備欄しか見えない**状態だった
   （design-system 項目3「停止中も常にプレイヘッドが見える」に違反）。
   `scrollToSongHead()` を追加し、`loadPreset()` と MIDI読み込み後の両方から呼ぶ。
   検証: 7曲を連続で切り替え、全曲で `x(0)` が可視範囲に入ることを確認。

2. **`@media (max-width:720px)` の `#stage{height:260px}` が完全に死んでいた**
   `js/stage.js` の `render()` が `const H = 360;` を持ち、`setupCanvas()` が
   **インラインstyleで毎回上書き**するため、CSS側は絶対に勝てなかった。
   → 高さの一次情報を CSS の `#stage-wrap { --stage-h }` に置き、`stage.js` が
   `getComputedStyle` で読む形にした。**JSに寸法の数値を持たせない。**

3. **舞台がリサイズで組み直されず、演者3が画面外に描かれていた**（`js/stage.js`）
   `prepare()` が `canvas.clientWidth`（＝前回 setupCanvas が書いたインライン幅＝**古い値**）を
   読み、`render()` は `parentElement.clientWidth`（新しい値）を読んでいた。両者が食い違い、
   縮小方向のリサイズで**レイアウトだけ古い幅のまま**になっていた。
   → `stageBox()` に一本化（幅=親、高さ=`--stage-h`）。375px幅で演者3が出ることを確認。

4. **狭い画面でタイムラインが折り返しの下だった**（`css/style.css`）
   375pxで `#score-area` の上端が994px（画面高812px）。`@media (max-width:720px)` で
   `main` をflex化し、**編成の設定を楽譜の下へ回す**（DOM順は変えない＝デスクトップは従来どおり）。
   → 上端 994px → 724px。design-system「720px以下では navigator・再生・時間表示・横操作を優先」。

5. **Shift+ホイールの横パンが未実装だった**（`js/main.js`）
   既定のままだと**ページ全体が縦に動くだけ**で楽譜は止まったままだった（実測で確認）。
   `#scroll-area` の wheel ハンドラに分岐を追加。⌘/Ctrl+ホイール（ズーム）は従来どおり。

### アクセシビリティ（Codexに実装させ、Claudeが検証・修正）

design-system「時間位置・ズーム・ループ範囲・再生状態をアクセシブルなラベルで露出する」への対応。

- `#pos-sr`（`.sr-only` の `role="status"`）を新設。**`#pos-readout` は `aria-hidden="true"`**。
  再生中は毎フレーム書き換わるため、そのまま読み上げ対象にすると破綻する。
  **読み上げ領域は停止中しか更新しない**（`updateReadout()` 内の `if (!playing)`）。
- ループ範囲は「解除時だけ通知」ではなく、**停止中の読み上げ内容に常時含める**
  （`位置　ループ ○小節○拍 から ○小節○拍`）。
  最初の実装ではsetLoop内で通知していたが、**直後の `updateReadout()` に上書きされて消えていた**
  （実機で発覚）。`loopLabel()` を作って読み上げ文字列に合成する形へ変更。
- 曲を切り替えるたびに `setLoop(null)` が走るため、**「ループ解除」は直前にループがあったときだけ**通知。
- `#btn-play` に `aria-pressed`、`#inp-zoom` に `aria-valuetext`（`1拍あたり○ピクセル`）。
- `#ruler-hit` は `role` のない `<div>` だと aria-label が読まれないため `role="group"` を付与。
- `@media (pointer: coarse)` で再生・ズーム・ループの各ボタンと `#inp-zoom` を44pxへ。
  **デスクトップ（pointer:fine）の見た目は変えない。** 実測でcoarse時に全て44pxを確認。

### 見送り（設計上の判断が要る／仕様が矛盾している）

- **Space+ドラッグでのパン**: design-system は「Space=再生/停止」と「Space+ドラッグでパン」を
  両方書いているが、**Spaceのkeydownで再生が始まるため両立しない**。中ボタンドラッグで代替済み。
- **狭い画面でのMIDI読み込み／書き出し／行動表をコピーの畳み込み**: これらは `#transport` の
  子要素なので、CSSの `order` だけでは楽譜の下へ回せない（DOM変更が要る）。
  折りたたむかどうかは体験の判断なので本人確認待ち。

### キャッシュ

`index.html` のバージョンクエリを更新（style.css v2→v5、main.js v3→v8、stage.js に v2 を付与）。
**GitHub Pagesは同一URLを長くキャッシュするため、JS/CSSを触ったら必ず上げる。**

## 作曲支援の測定器と新曲「重ね鈴」（2026-08-23）

保持キャッチ和音は**受動的にしか発動しない**（スケジューラーがたまたま見つけたときだけ）。
書いた和音が1手で成立したのか、単に2人へ振り分けられたのかは、ブラウザで行動表を目視するまで
分からなかった。**狙って和音を書くには、まず測れる必要がある。** そこから作った。

### `tools/analyze.mjs`（測定器）

ブラウザを介さず `js/scheduler.js` を直接回す。`js/presets.js` だけ `window` と
`localStorage` に触るのでシムを入れている（scheduler.js と songs.js はDOM非依存）。

```
node tools/analyze.mjs                  全曲サマリ（リング数・パス率・振り・不可能・和音成立数）
node tools/analyze.mjs kasanesuzu       1曲の詳細（和音の箇所ごとに ○× と理由）
node tools/analyze.mjs gomon --seeds 60 シードを振って成立の安定性を見る
node tools/analyze.mjs --file draft.json 作曲中の曲を外から与えて試し打ち
```

不成立のときは**原因を切り分けて出す**のが要点:
- 「そもそもどの音高のリングも手に静かに置かれていない → 作曲で直せる」
- 「素材はあった → 分離経路か投げ側の都合で不採用」（作曲では直しにくい）

判定は `actions` を見る。`noteResults` の `chord` は**持っていた側にしか付かず**、
新しく取った側は `chordRole:"new"` のアクションにしか出ないため。

### `tools/invariants.mjs`（不変条件スキャン）

リング参照の健全性と「同じ手が同時に2本を持っていないこと」を多シードで確かめる。
**保持キャッチ和音の瞬間だけは1手に2本あるのが正しい状態**なので違反にしない。
これを外していないと、和音のある曲だけが軒並みNGに見える（最初これで誤検知した）。

```
node tools/invariants.mjs             全曲を実使用の20シードで
node tools/invariants.mjs --seeds 200 採用されない領域まで広げる
```

### 測って分かった作曲の規則（2026-08-23 実測）

- **和音の「持たれる側」の音高は、和音の4拍前に鳴らしておく。** そのリングが手に残ったまま
  和音の時刻を迎える。**8拍以上空けるとスケジューラーが脇へしまってしまい、素材が消える**
  （草稿v1の失敗はこれ。8拍前・12拍前に鳴らしていた）。
- **★和音とパス率は競合する。** 和音は「手の中で休んでいるリング」を要求し、パスは
  「リングを回し続ける」ほど増える。同じ旋律の変種で実測:
  リング6本・パス88%・和音4/6 ⇔ リング8本・パス70%・和音6/6。
- **同じ音高のリングが複製される（レ×2 等）と和音が出やすい。** 片方が働き、片方が休めるため。
  つまり和音を狙うと自然にリングが増え、その分パスが減る。
- テンポと滞空には**平坦な当たり領域**がある（66〜70BPM × 滞空1.2〜1.4 でどれも和音6/6・パス70%）。
  端（58BPM や 74BPM）では崩れる。作曲後に `--file` で総当たりして確かめるとよい。
- `check_song()` の同音間隔警告は**和音の拍では偽陽性**（保持キャッチ和音なら投げ直しが不要）。
  同時刻2音の拍を警告から除外するよう `make_midi.py` を修正した。

### 新曲「重ね鈴（かさねすず）」

66BPM・4拍子・3人・滞空1.3秒。ラドレミソ（ヨナ抜き短音階）の5音、1音2拍の静かな稿。
**技法を先に置いてから旋律を書いた最初の曲。** 同時刻2音を6箇所書き、**6箇所すべてが
1手の和音として成立する**（既存曲は最多でも3箇所）。リング8本・パス70%・振り0・不可能0。

- 和音の並び: ミ+ソ → ラ+ミ → ド+ソ → レ+ソ → ド+ミ → ラ+ド+ミ（終止）。
  すべて協和音程（3度・4度・5度）に絞り、2度・7度は避けた。
- MIDI連番を動かさないため `SONGS` の**末尾**に追加している（`19_kasanesuzu.mid`）。

### 検証

- 全19曲、実使用の20シード（`TOREI.schedule` が選ぶ範囲）で不変条件の違反 0件。
- 全19曲で振り0・不可能0。書かれた和音 20箇所中 19箇所が1手和音として成立。
- 行動表テキストに和音の行が12行（6和音×2行）出ることをブラウザで確認。
- ナビゲーター帯に赤点（振り・不可能）が1つも出ないことを目視で確認。

### 見つけた別件（既存・未修正）

`メリーさんのひつじ` は **seed 39 / 51 / 78** で「同じ手が2.5秒間2本を保持する」経路を作る
（和音ではない）。`TOREI.schedule` は seed 0〜19 しか使わないため**アプリの挙動には出ない**が、
シード数を増やす変更をすると表面化しうる。`node tools/invariants.mjs --seeds 200 mary` で再現。

## 編集した曲を残す（2026-08-23）

手直しがリロードや曲切替で消えてしまう問題への対応。**保険（自動保存）と、残したいものを
外に出す手段（JSON）**の2段構えにした。実装はCodexに投げ、Claudeが実機で検証・修正。

### 自動保存（localStorage `torei.work`）

- `recompute()` の末尾で **400msデバウンス**して保存する。recompute はノート編集・設定変更・
  曲切替・MIDI読み込みのすべてが通る要所なので、ここ1箇所で全部拾える。
  デバウンスはドラッグ中にlocalStorageを叩き続けないため。
- 起動時、有効な保存があれば `applySongData()` で復元し、無ければ従来どおり `loadPreset("saints")`。
- **★復元したとき曲セレクタを「白紙」に戻している。** 元の曲名が選ばれたままだと、
  それを選び直しても `change` イベントが発火せず元の曲に戻せない
  （＝「曲を選び直すと破棄されます」という案内が嘘になる）。編集の有無は
  bpmと音符列を元プリセットと比べて判定し、編集されていれば白紙にする。
  Codexの初期実装はここが未対応で、実機で確認して直した。
- localStorage が使えない環境（プライベートモード等）でも落ちないよう `js/songfile.js` 側で
  すべて try/catch。`load()` は失敗時 null。

### JSON入出力（「曲を保存」「曲を開く」）

- **形式は `tools/analyze.mjs --file` 互換**にしてある。これが要点で、
  `ブラウザで手直し → 曲を保存 → node tools/analyze.mjs --file で和音チェック → make_midi.py へ`
  が一本の流れになる。
- MIDI書き出しと違い、**編成の設定も丸ごと入る**（演者数・滞空・脇・スタンド・パッシング・
  同音リング最大・振りの可否）。MIDIは音符とテンポしか運べない。
- `analyze.mjs` の `cfgFor()` も `wakiCap` / `maxDup` / `allowShake` を読むようにした
  （無ければ従来の既定値）。
- 検証は `js/songfile.js` の `validate()` に集約。不正なら state を一切触らずに
  `showNotice(..., true)` で理由を出す（実機で「壊れたJSON」「bpm範囲外」「midi範囲外」の
  3パターンが state を変更しないことを確認）。

### 検証（実機）

- localStorage を空にして起動 → 聖者の行進・復元通知なし。
- 重ね鈴を選びBPMを72へ → 700ms後に `torei.work` に保存されている。
- リロード → BPM72で復元、セレクタは白紙、通知に元の曲名が入る。
- そこで重ね鈴を選び直す → BPM66の元の曲に戻る（破棄経路が機能する）。
- 書き出したJSONは13キーすべて揃い、読み込みで9つのUI入力すべてに反映される。
- 書き出したJSONを `node tools/analyze.mjs --file` に直接渡して和音6/6を確認。
- コンソールエラー0件。デスクトップのレイアウトは不変。

### 既知の副作用・割り切り

- **モバイル375pxで `#score-area` の上端が 724px → 774px に下がった**（ボタン2つ分）。
  画面高812pxにはまだ収まるが、楽譜の見えている量は減った。保留中の
  「狭い画面で二次操作（MIDI・コピー・保存）を畳むか」を入れれば戻る。
- 復元後はセレクタが白紙なので、その状態で「曲を保存」すると JSON の `name` が
  「（白紙から作る）」になる。`notes` と設定は正しいので実害はない。

## 検証ツールの検証（メリーさんのひつじの「バグ」の顛末）（2026-08-23）

前回、`node tools/invariants.mjs --seeds 200 mary` が「メリーさんのひつじが seed 39/51/78 で
同じ手に2本を保持する」と報告し、それを既知の未修正バグとして記録した。今回、実際に潰そうと
原因調査に入ったところ、**バグは `js/scheduler.js` ではなく検証ツール自身にあった**と判明した。
誤った実装を「既知の不具合」として記録しかけていた話なので、経緯を残す。

### 追跡の結果

`tools/invariants.mjs`（および `tools/analyze.mjs` の `inHandIntervals`）は、ある手が
「今どのリングを持っているか」を actions 列から再構成するために、時刻でイベントをソートして
in（取得）/out（手放す）を交互に対応づけていた。ここに**浮動小数点の桁落ち**があった:

- リングを取得し終える時刻は `pickup.t + pickup.dur` で計算する。
- 同じリングをすぐ投げる時刻は `throw.t`（スケジューラー内部では算数的に同じ値のはず）。
- 実測: `2.9000000000000004 + 0.7 === 3.6000000000000005`、一方 `throw.t === 3.6`。
  **最下位ビットが一致しない。**
- ソート `list.sort((x,y)=>x.t-y.t)` はこの2つを「ほぼ同時刻」ではなく「out(3.6)がin(3.60000...05)
  よりわずかに早い」と判定する。結果、out イベントが「対応する開始がない」として握りつぶされ
  （`else if (open)` が false）、直後の in イベントが新しい区間を開き、それが**本来は数秒後に
  閉じるはずの別の区間の open と誤って合体**した。これが「2本を同時に保持している」ように
  見える偽の区間を作っていた。
- 実在のスケジューラーの状態（`js/scheduler.js` の `possConflict`／`hand.poss`）自体には、
  この計算誤差は影響しない。`possConflict` はリアルタイムの区間比較で、EPS(1e-6)による
  許容誤差を最初から持っている。**問題は事後の"検証"コードだけが素の等値比較に近い形で
  タイミングを扱っていたこと。**

### 修正

`tools/invariants.mjs` と `tools/analyze.mjs` の両方で、in/out イベントの時刻を
`Math.round(t * 1e6) / 1e6`（マイクロ秒単位に丸め）してからソートするようにした。
これで真に同時刻のイベントは確実に同値と判定され、挿入順（＝スケジューラーが実際に
処理した順）を安定ソートが保持する。

### 検証

- 修正後、`node tools/invariants.mjs --seeds 200 mary` → **違反0件**（200シード全て）。
- 全19曲を300シードで再スキャン → **違反0件**。
- 「重ね鈴」も改めて100シードスキャン・不変条件200/300シードともに問題なし
  （測定結果そのものは前回と変化なし。今回のバグは mary の特定の音配置でだけ
  顕在化する偶発的な浮動小数点の巡り合わせだったため）。

### 教訓

検証コード自身にもバグは入る。**「怪しい結果が出たら、まず疑うべきは調べている対象ではなく
調べ方」**という順番を踏み外しかけた。今回は本格的な修正に入る前にトレース用のログを
仕込んで実際の内部状態を1ステップずつ確認したため、存在しないバグを`js/scheduler.js`に
書き込まずに済んだ。**「既知の不具合」として記録する前に、一度は原因を実際に特定してから
書く**（伝聞や1回の測定結果だけで確定事実として記録しない — CLAUDE.mdの基本方針そのもの）。

## 「重ね鈴」の手直し（2026-08-23・技術検証と構造レビューに置き換え）

本人から旋律の「手直し」を依頼されたが、**Claudeには音声を実際に聴く手段がない**。
実演奏の可否を確かめる方向で代替した:

- **技術検証**: ブラウザで実際に再生し、AudioContextが `running` になること・
  6箇所の和音すべてが行動表に和音として出ることを確認（既存の測定器の再確認）。
- **構造レビュー**（聴覚の代わりに理論面で検証）:
  - 隣接音の跳躍をペンタトニック度数（半音ではなく ドレミソラ の5度数）で分類。
    28回の遷移中、隣接1度=13・2度=10・3度以上=5。**極端な跳躍はなく、順次〜小跳躍が主体。**
    （半音差だけで見ると「跳躍だらけ」に誤読する——ペンタトニックは音同士の間隔が
    半音3つ分空くことがあるため。度数ベースで見直す必要があった。）
  - 4小節ごとのフレーズを比較: Phrase2「ソレラレミドソミ」とPhrase3「ソレラレミソドソ」が
    **前半5音が完全一致、後半で分岐する変奏**になっていた（意図した設計どおり）。
    Phrase4はPhrase1の開始動機を短く回収する終結部。**ABB'A'に近い、意図の通った形式**。
  - 終止和音（56拍目）はラ+ド+ミ＝主音ラ上の三和音。曲全体がヨナ抜き短音階（ラドレミソ）
    である以上、**理論上もっとも収まりのよい終止**。
- **結論**: 構造・跳躍・終止のいずれにも欠陥は見当たらなかった。**根拠のない変更を
  加えるより、測って問題がなかったことをそのまま報告する方を選んだ**（CLAUDE.mdの
  「機能を削るのはよいが中心体験を粗雑にしない」「不要な変更を足さない」の両方に当たる）。
  実際に聴いた手直しではないため、**演奏して気になる箇所があれば個別に直せる**
  （測定器があるので和音を壊さず調整できる）。

## 狭い画面での二次操作の畳み込み（2026-08-23・design-system.md 保留分の解消）

「曲データを保存」「曲データを開く」の追加でモバイルの楽譜表示領域がさらに狭くなっていた
（`#score-area` の上端が774px）。MIDI読み込み・書き出し・行動表をコピー・曲を保存・曲を開く
の5つを `#transport-more` としてまとめ、`@media (max-width:720px)` でのみ折りたたむ。

- `#transport-more` は常時 `display: contents`（自身は箱を作らず、`#transport` の
  `gap` によるボタン間隔をそのまま保つ）。狭い画面でだけ `:not(.open) > *` を非表示にする。
  デスクトップは常時展開・トグルボタン自体が非表示のまま（見た目は完全に不変）。
- トグルボタン（`#btn-more-toggle`）はデスクトップでは常時非表示、モバイルでのみ出現。
  `aria-expanded` / `aria-controls` を付け、クリックで `.open` クラスをトグルしてラベルを
  「その他の操作 ▾」⇄「操作を閉じる ▴」に切り替える。`@media (pointer: coarse)` の
  44px対象にも追加した。
- **実機で確認**: 折りたたみ時、`#score-area` の上端が774px → **681px**（曲を保存/開く追加前の
  元の724pxより良い値）。展開すると全ボタンが現れ、再度クリックで畳める。
  デスクトップ1100pxでは全ボタン常時可視・トグル非表示を確認。コンソールエラー0件。

## 本人指示による5点の変更（2026-08-23）

### 1. テンポをスライダーに

`<input type="number">` → `<input type="range" min=30 max=200 step=1>` ＋ 数値表示（`#bpm-val`）。
- **min は40ではなく30**。きらきら星が36BPM、疾風が187BPM で、旧来の 40〜200 では
  きらきら星がスライダーの範囲外だった。
- 反映は `change` ではなく **`input`**（滞空スライダーと同じ作法。動かしながら結果が変わる）。
- BPM を書き込む経路が3つ（`loadPreset` / `applySongData` / `onMidiFile`）あり、
  スライダーと数値表示の両方を更新する必要があるため **`setBpmUI(bpm)` に集約**した。
  片方だけ更新する実装にすると、曲を変えたときにつまみと数字がずれる。

### 2. 警告をクリックしてその箇所へ飛べるように

警告は「読むもの」ではなく「潰していく作業リスト」。1行を `<button class="warn-jump">` にして、
クリックで `seek(w.t, {center:true})` → 楽譜を画面内へスクロール → **プレイヘッドを0.9秒明滅**
（`#playhead.flash`）。飛んだ先が分かるようにするため。
- `msg` は HTML に埋め込むのでエスケープしている。
- ボタンだが見た目は「文章の行」にしたいので、共通の `button` スタイル（枠・中央寄せ・
  字送り）を `.warn-jump` で打ち消して左揃えの行に戻している。

### 3. 投げの高さを段階（1〜5）で表す

稽古で声に出すのは「1.15秒」ではなく「高さ4」。`TOREI.throwLevel(flightSec)` を
`js/presets.js` に置き、**滞空 0.7〜1.4秒を0.15秒刻みで5段**に割る。

| 段 | 滞空 | 頭上 |
|---|---|---|
| 高さ1 | 0.70〜0.84秒 | 約0.6〜0.9m（低く速い） |
| 高さ2 | 0.85〜0.99秒 | 約0.9〜1.2m |
| 高さ3 | 1.00〜1.14秒 | 約1.2〜1.6m |
| 高さ4 | 1.15〜1.29秒 | 約1.6〜2.0m |
| 高さ5 | 1.30〜1.40秒 | 約2.1〜2.4m（高く大きい） |

- 設定欄の表示を「＝ 高さN（頭上 約Xm ／ 床から 約Ym）」に。
- **行動表の投げ行すべてに「高さN」を入れた**。スケジューラーは1投ごとに滞空を変える
  （`plan.flight`）ので、実際に段が混ざる。冒頭に段の対応表も出す。
- 定義を `js/presets.js` の1箇所に置き、設定欄と行動表が同じ関数を見るようにしている。

### 4. ★振りはフレーズ末のみ・多用不可（演出上の方針）

振り（手元で鳴らす）はジャグリング要素がないので本人は好まない。ただしフレーズ末なら
「余韻を鳴らす所作」として演出上成立する。そこで**フレーズ末に限定**した。

**フレーズ末の定義**: 次の打点までが「直前までの音の流れの2倍以上」**かつ**「3拍以上」空く音、
および曲の最後の音。両方を課したのは、片方だけだと不安定だったため（全19曲で実測）:
- 倍率だけ（2倍）→ 蛍の光が50打点中21箇所＝42%がフレーズ末になり、限定にならない
- 拍数だけ → もともとゆったりした曲では全部の音がフレーズ末になる
- 両方 → 1曲あたり**1〜9箇所**に収まる（規則的な曲＝五音の橋・宵待ち・重ね鈴 は最終音のみ）

**上限**: フレーズ末に限っても連発すればジャグリングでなくなるので、
**フレーズ末の数の1/4（最低1）本まで**。超えた分は成立させず「不可能」として警告に出す。

**警告の説明を厚くした**。振りをONにしているのに使われない理由が分からないと詰むので、
不可能になったとき「フレーズ末ではないので振りでの代用は使えません」
「振りは既に上限N本に達しています」を付ける。

検証:
- 収録19曲すべて 振り0・不可能0 のまま（この変更で壊れた曲はない）。
- 聖者の行進を1人・2人に絞って破綻させると、以前なら振りで埋めていた箇所が
  すべて「不可能＋フレーズ末ではない旨」になることを確認。
- フレーズ末の音だけが投げで間に合わない旋律を自作し、**そこでは振りが1本出る**ことを確認
  （`allowShake=false` にすると同じ箇所が不可能になる＝ルールが効いている）。
- フレーズ末が5箇所ある旋律で上限が2ではなく1（ceil(5/4)=2 のはずが、
  実際に検出されたフレーズ末は4箇所で ceil(4/4)=1）になり、2本目以降が
  「上限1本に達しています」で弾かれることを確認。

### 5. ヘッダーのタグライン削除

「投げてから、鳴るまで。」を削除（`.tagline` の p要素）。CSSの `.tagline` ルールは
他で使う可能性を考えて残してある。

### ついでに直したもの

`@media (pointer: coarse)` の44px対象に **`#inp-bpm` と `#inp-flight` を追加**。
テンポをスライダーにしたことで、指で掴む対象が1つ増えたため（実測16px → 44px）。
モバイルでの `#score-area` 上端は 681px → 708px（スライダー高さの分）。折り返し内に収まる。

## パスの重み付け強化（2026-08-23 本人要望「演者間のパスをよしとする重み付けを」）

パス率が全体65%まで下がっていた（機能追加を重ねる中で、採点がパスを十分守っていなかった）。
3点セットで **65% → 78%（全体）、不可0・振り0のまま、和音は19/20 → 20/20 に改善**。

### 変更の設計（js/scheduler.js）

1. **passCost をseed依存に**: 「多め」時の passCost を、強気シード -0.7 ／ 従来シード -0.1 の
   2群に分ける。**全シードを強気にすると Swing Low が20シード全滅**する
   （パス95〜97%まで振れて自分の担当音を出せなくなる）。
   - ★分け方は `floor(seed/3) % 2`。**avoidLevel（seed%3）と直交させる**のが要点。
     最初 `seed%3===2` を従来群にしたら、Swing Low の成立シード（6,12,15,18 — すべて
     avoidLevel=0 の群）を1本も含まず安全網にならなかった。直交させれば
     全avoidLevel × 両passCost の組み合わせが必ず試される。
2. **採点のパス率重みを 5 → 8**（`passBonus`）。
3. **採点に和音の欠けを追加**（`chordMiss × 3`）。これまで採点に和音が入っておらず、
   パスを強めると「静かに持つ」状態が減って**和音を潰すシードが選ばれていた**。
   採点に入れたことで、宵待ちの唯一の不成立和音まで成立するようになった（20/20）。
   和音1箇所 ＞ リング2本（0.1×2）という重み設計なので、グリーンスリーブスでは
   和音1/1を守るためリング13→15本を受け入れる（元から限界例の曲。設計通りの動作）。

### 実測（主な変化。node tools/analyze.mjs）

| 曲 | パス率 | 備考 |
|---|---|---|
| 聖者の行進 | 69→**94%** | |
| きらきら星 | 64→**88%** | リング6→8本 |
| 疾風 | 51→**97%** | |
| アメイジング・グレイス | 52→**87%** | |
| きよしこの夜 | 74→**91%** | リング11→10本 |
| 投鈴ワルツ | 45→**86%** | |
| 宵待ち | 81→**95%** | **和音2/3→3/3** |
| 蛍の光 | 72→66% | リング8→**6本**に減（採点上のトレード） |
| Swing Low | 69% | 変化なし（強気シードでは成立しない曲） |
| 重ね鈴 | 70% | 変化なし（和音6/6を守る方が採点で勝つ） |

- 全19曲・200シードで不変条件の違反0件。重ね鈴の和音安定性も維持（60シードで平均5.0/6）。
- `make_midi.py` の desc に書いてあった旧実測値（10曲分）を新しい値に更新。
  `js/songs.js` は再生成しても差分なし（descは生成物に含まれない）。

### 学び

- **採点に入っていない品質は、他の品質を強めた瞬間に壊れる。** パスを強めるまで和音は
  「たまたま」守られていた。守りたいものは全部採点に入れる。
- **シードを群に分けて両極を探索する際は、既存の群分け（avoidLevel）と直交させる。**
  同じ剰余系で分けると、特定の組み合わせが一度も試されない死角ができる。

## 拍子セレクタ（2026-08-23 本人要望「4/4ばかり。3/4やその他有名な拍子に変えられるように」）

`#sel-meter` を「曲」セレクタの隣に追加。選択肢は 4/4・3/4・2/4・6/8・5/4。

- **内部の「拍」は抽象単位**なので、実装は `state.melody.beatsPerBar` を切り替えるだけ。
  ルーラーの小節線・小節番号、位置読み出し（○小節○拍）、「この小節を反復」の幅、
  Shift+矢印の移動量が全部 `beatsPerBar` をライブで読んでいるため自動で追随する。
  音符の時刻（拍）は動かない＝拍子を変えても曲は同じに聞こえ、**小節の数え方だけが変わる**。
- 値を書き込む3経路（曲選択・JSON読み込み・MIDI読み込み）から `setMeterUI()` を呼ぶ
  （`setBpmUI` と同じパターン）。MIDI由来の 7/8 など選択肢にない拍子は動的に
  「7拍子」オプションを作って選ぶ。
- **6/8 のMIDI書き出し**: 拍子メタ 0x58 の分母が4分音符（2^2）固定だったため、
  6拍/小節を選ぶと 6/4 として書き出されていた。beatsPerBar===6 のとき分母 2^3（8分）に
  するよう `js/midi.js` を修正。実バイト検証済み（6/8 → [6,3]、4/4 → [4,2]）。
- 自動保存・JSON入出力は songfile が既に `beatsPerBar` を持っているので変更不要
  （実機で拍子変更→700ms後の自動保存に beatsPerBar:3 が入ることを確認）。
- 検証: 投鈴ワルツ選択でセレクタが「3/4」、グリーンスリーブスで「6/8」に追従。
  3/4に変えるとルーラーの小節番号が3拍間隔になる（目視）。「この小節を反復」は
  `loopCurrentBar` が bpb をライブで読むので反復幅も 4拍/3拍/6拍と正しく変わる。
  コンソールエラー0件。

## サンプル15曲の追加（2026-08-23 本人要望「みんなが知っている既存曲で15曲」）

「良いものをピックアップしてデザインしていく」ための素材。全曲パブリックドメイン
（作曲者没後70年超または伝承曲）。**音列の裏取り方針**: Webのドレミ譜で照合できた曲（★）と、
超定番で記憶が確実な曲（☆）だけを採用。**裏取りが取れなかった曲は採用しない**
（茶摘み・浜辺の歌・通りゃんせ・線路は続くよ・桃太郎は音列ソースが見つからず見送り）。

### 収録（全曲 振り0・不可能0）

| 曲 | 出典 | 拍子 | BPM | 人 | パス | 特徴 |
|---|---|---|---|---|---|---|
| さくらさくら | ★co0-sky | 4/4 | 42 | 3 | 79% | 陰旋法5音（ミファラシド）・和風 |
| ふるさと | ★知恵袋 | 3/4 | 34 | 3 | 73% | 3拍子唱歌の代表・音高7 |
| ぶんぶんぶん | ★知恵袋 | 2/4 | 96 | 3 | 84% | 5音高・リング6本・軽快 |
| 夕焼け小焼け | ★知恵袋 | 4/4 | 60 | 4 | 69% | ヨナ抜きだが音域2オクターブ弱で音高11＝4人 |
| 大きな古時計 | ★知恵袋 | 4/4 | 60 | 4 | 67% | 音高9・前半+サビ |
| かえるの合唱 | ☆独民謡 | 4/4 | 68 | 3 | 60% | 輪唱定番・クワクワの同音連続 |
| ちょうちょう | ☆独民謡 | 4/4 | 60 | 3 | 54% | 5音高 |
| ロンドン橋 | ☆英伝承 | 4/4 | 96 | 3 | 58% | 6音高・96BPMで成立 |
| ジングルベル | ☆ピアポント | 4/4 | 60 | 3 | 71% | 鈴の曲＝投鈴向き・サビのみ |
| ハッピーバースデー | ☆ヒル姉妹 | 3/4 | 34 | 3 | 64% | 誰もが歌える・音高8 |
| ゆかいな牧場 | ☆米民謡 | 4/4 | 60 | 3 | 71% | ヨナ抜き5音 |
| もろびとこぞりて | ☆賛美歌 | 4/4 | 66 | **4** | **96%** | 音階下降が名刺代わり・パス率全曲最高 |
| 草競馬 | ☆フォスター | 4/4 | 48 | 3 | **91%** | ヨナ抜き5音・リング6本 |
| こげこげボート | ☆英米伝承 | **6/8** | 48 | 3 | 65% | 6/8の代表・輪唱 |
| グーチョキパー | ☆仏伝承 | 4/4 | 42 | 3 | 69% | フレール・ジャック・輪唱 |

### 作り方（再現手順）

1. 音列をWebのドレミ譜（知恵袋・ドレミ譜サイト）で裏取り → `make_midi.py` に採譜
   （リズムは投鈴向けに単純化: 8分は1拍化・フレーズ間に休み。音列は原曲どおり）。
2. **自動チューナー**（/tmp/tune_new.mjs 方式）で bpm × flight を総当たり。
   選好は「成立（振り0・不可能0）∧ リング≤人数×3 の中でパス率最大、同率なら速いテンポ」。
   - ★初回チューナーの選好を「成立する最速テンポ」にしたら、リング上限超過
     （birthday 14本/上限9）やパス0%（草競馬）を平気で選んだ。**何を最大化するかを
     明示しないと、成立ぎりぎりの粗い解が出る。**
3. 3人の上限内で成立しない曲だけ4人化（もろびと・夕焼け・古時計）。

### 学び

- **1音1拍×90BPM前後はほぼ全滅**（15曲中9曲に不可能）。実測どおり「1音2拍以上か、
  ゆったりテンポ」が3人成立の条件。34〜60BPMまで落とすと軒並み成立し、パスも増える。
- 34BPM（ふるさと・バースデー）はきらきら星36BPMの前例に沿う遅さ。実演では
  「1拍=1.76秒のゆったりした投げ交わし」になる。
- 音高11の夕焼け小焼けはオクターブ畳みも試みたが、旋律の起伏が壊れるためやめて4人化した。

## 小節の挿入・削除（2026-08-23 本人要望）

楽譜編集に「バーのところに小節を入れる（あとをずらす）／小節を消す（あとをつめる）」を追加。
`#score-toolbar` の「この小節を反復」の隣に「＋小節を挿入」「−小節を削除」。
どちらも**再生位置の小節が対象**（「この小節を反復」と同じ流儀）。

- **挿入**: 現在小節の頭から後ろの全音符を +1小節（beatsPerBar拍）ずらし、空の小節を作る。
- **削除**: 現在小節内の音符を消し、以降を −1小節つめる。
- **ループの追従**: 挿入点より後ろのループはそのままずれる（実測: 2小節目のループが
  1小節目への挿入で3小節目へ移動し、削除で2小節目へ戻る）。削除した小節にかかる
  ループは意味が変わるので解除する。
- **1段だけの「取り消す」**: 削除は音符が消える破壊的操作なので、直前の状態
  （音符・ループ・再生位置）を1段だけ保持し「取り消す」ボタンで戻せる。
  ★スナップショットは**小節操作以外の編集が入った時点で無効化**する（recompute に
  `inBarOp` フラグを通す）。これをしないと、音符を編集した後に古いスナップショットで
  新しい編集ごと巻き戻す事故が起こる。
- 拍子セレクタと連動: 3/4 なら3拍単位、6/8 なら6拍単位でずれる（bpbをライブで読む）。

検証（実機・聖者の行進）: 挿入で冒頭の音符 beat 0,1,2,3 → 4,5,6,7・音数50不変・
TOTAL_BEATS 100→104。削除で50→46音・次の音符が beat 4へ。取り消しで完全復元。
BPM変更後に「取り消す」が消えることを確認。モバイルで両ボタン44px。コンソールエラー0件。

## 4人以上前提の曲を非表示に（2026-08-23 本人指示）

対象5曲: きよしこの夜・グリーンスリーブス・夕焼け小焼け・大きな古時計・もろびとこぞりて
（いずれも `performers: 4`）。**曲リスト（プルダウン）から選べなくするだけで、
データ自体は削除していない。**

- `tools/make_midi.py` の該当5エントリに `"hidden": True` を追加。`make_songs_js()` が
  `hidden: true/false` を全曲へ出力するようにした（`js/songs.js` は生成物、直接編集しない
  という既存ルールのまま）。
- `js/main.js` の曲プルダウン構築で `p.hidden` を除外。**`TOREI.SONGS` 自体は触っていない**
  ので `tools/analyze.mjs`・`tools/invariants.mjs`（`TOREI.SONGS` を直接見る）は
  従来どおり34曲全部を測定できる。「見えなくする」と「消す」を分けた。
- 復元経路（JSON読み込み・自動保存からの復元）で非表示曲のidに当たったとき、
  プルダウンに該当`<option>`が無いままセレクタへ値を代入すると空欄化する事故があるため、
  判定を `TOREI.PRESETS.some(...)` から**実際にドロップダウンへ出ている`<option>`の有無**
  （`[...sel.options].some(...)`）に変更した。音符・設定自体は`data`から正しく読み込まれる
  （非表示曲のJSONを読み込んでも「白紙」表示のまま中身は正しく反映されることを確認）。

検証: プルダウンが30件（29曲+白紙）に減り、対象5曲が含まれないことを確認。
`TOREI.SONGS.length` は34のまま、`hidden:true`の5曲IDも取得できる。非表示曲のJSONを
読み込むとセレクタは「白紙」になるが音符43個・設定は正しく反映される。
`node tools/analyze.mjs` は引き続き34曲全部のサマリを出す。コンソールエラー0件。

**元に戻す方法**: `tools/make_midi.py` の該当行から `"hidden": True,` を削除して
`python3 tools/make_midi.py` を再実行するだけ。

## 滞空候補の刻みを0.05に（2026-08-23 本人指摘「パスがやたらと少ない。ぶんぶんぶんを修正したい」）

### 原因（2段階）

1. **表面**: ぶんぶんぶん（96BPM）のパス率は滞空=ちょうど1.00のときだけ84%で、
   ±0.05ずれると19〜53%に崩落する崖だった。滞空スライダー（0.05刻み）を少し
   動かすだけで「やたらと少ない」状態になる。
2. **根**: `planToss` の滞空候補が「基準値から**0.1秒刻み**」だったため、スライダーが
   0.95のとき 1.00 の成立窓を**一度も試せない**（0.95→0.85→1.05…と跨いでしまう）。
   テンポが速い曲ほど成立窓が狭く、この量子化が崖を作っていた。

### 修正

`fCands` の刻みを 0.1 → **0.05** に。どのスライダー位置からでも隣の成立窓に届く。
実測（ぶんぶんぶん）: 0.95〜1.05 が84%の平坦域になり、0.85/0.90 も22%→53%へ底上げ。
アプリ実機でもスライダーを0.95/1.05に動かして84%を維持することを確認。
探索候補は倍増するが全34曲の解析が0.8秒で終わる（性能問題なし）。

### 副作用（受け入れた）

探索順が変わるため採用シードが変わる曲がある。**聖者の行進が94%→69%に低下**
（滞空をどう振っても69%が上限。94%は旧探索順での偶然の当たりで、初期リリース水準に
戻った形）。グーチョキパーは69%→75%に上昇。他の32曲は維持。
全曲で振り0・不可能0・和音20/20は変わらず。20シードの不変条件も違反0件。

### 学び

**連続量のパラメータをグリッドで探索するとき、グリッドの位相が結果を支配することがある。**
「基準値±k×step」型の探索は、基準値がグリッドからずれると良い解に到達できない。
刻みをUI操作の粒度（スライダーの0.05）に合わせるのが正しい。

## ぶんぶんぶんのリズム復元（2026-08-23 本人指摘「5-8小節部分をなんとかしたい」）

サンプル採譜時の「8分音符を一律1拍化」が原因で、中間部（おいけのまわりに〜）が
原曲の2倍に間延びし、さらにフレーズ間の空白小節が挟まって5小節目「ドー」の後に
死んだ小節ができていた。

修正: **8分音符を0.5拍で書く**原曲どおりの12小節形へ。
- 冒頭「ぶんぶんぶん」=4分（1拍）、「はちがとぶ」＝8分×4+2分、中間部＝8分×16（4小節）。
- bpm 96→48 だが、**8分の実時間は旧96BPM・1拍版と同じ0.625秒**。聞こえる速さは
  中間部で同じ・冒頭だけ原曲どおりゆったり2倍になる（旧版は全音符同速＝冒頭が
  原曲比2倍速のリズム比率だった）。
- チューニング: bpm×flight 総当たりで 48BPM/滞空0.95。パス75%（旧84%からの微減は
  リズム修正の代償）・リング6本・**滞空±0.1で69〜75%と頑健**（0.05刻み化の恩恵）。
- 検証: 全12小節・空白小節ゼロ・5〜8小節は各4音の8分の走り。全34曲の回帰も
  振り0・不可能0・和音20/20のまま。不変条件も違反0件。

教訓: **採譜の量子化はリズムの「比率」を保存しないと曲が壊れる。** 8分と4分が混ざる曲を
一律1拍にすると、速い部分だけが間延びして原曲に聞こえなくなる。今後のサンプル採譜は
8分=0.5拍を基本にする（他の初期サンプル曲にも同じ問題が残っている可能性がある。
特に かえるの合唱の「ケロケロ」、ちょうちょう、ロンドン橋、ジングルベルあたりは
8分を含む原曲）。

## Qシート（演者ごとの手順表）と ?song= リンク（2026-08-23 本人要望）

クライアントに「ぶんぶんぶんをセットした状態で送りたい」「演者が何をするか学習できる
インターフェース（Qシート）はあるか」→ Qシートは未実装だった（design-system.md に
「将来の演者ごとのキューシート」とあった項目）ので、この機会に両方実装した。

### ?song= URLパラメータ

`?song=bunbun` のように曲IDを指定すると、その曲をセットした状態で開く。
**自動保存の復元より優先**（リンクを受け取った人が確実にその曲を見られるように）。
非表示曲や不正なIDは無視して通常起動。
★`loadPreset()` はセレクタ表示を触らない（changeイベント経由の設計）ので、
URL起動時は `sel.value` を明示的に設定する必要があった。

クライアント向けリンク: `https://aratama-ship-it.github.io/bell-toss/?song=bunbun`

### Qシート（`#cuesheet`）

楽譜の下に常設。演者タブ（名前は編集内容に追従）＋時刻順の表
「小節｜時刻｜手｜すること」。データは `TOREI.cueSheet(result, melody, cfg, perfId)`
（scheduler側・actionText と文言を揃えた1人分フィルタ）、UI側は表にするだけの分担。

- 行の例: 「2小節2拍｜0:04.0｜右手｜レ を投げる（高さ2）→ 演者3へパス」
  「3小節1拍｜0:05.6｜右手｜演者3から ミ② をキャッチ → ♪ミ」。
  **キャッチ行は太字**（音が鳴る瞬間＝演者が最優先で覚える行）。準備ゾーンの行は薄く。
- **再生ハイライト**: 再生中は「いま実行中（直近に過ぎた）行」を真鍮色で強調し、
  中央へ自動スクロール。シークでも追従（`updateCueHighlight` を tick と seek に接続。
  差分更新のみで毎フレームのDOM書き換えはクラス2箇所だけ）。
- 検証: ぶんぶんぶん演者1=31行・演者2=28行、タブ切替、シークで 0:05.6→「ミ②をキャッチ」
  0:10.6→「ファをキャッチ」と正しく移ることを確認。コンソールエラー0件。

## サンプル曲のリズム点検（2026-08-23 ぶんぶんぶん修正の続き）と Qシートのスクロール修正

### Qシートのスクロール修正（本人指摘「再生中に画面が下にズレて舞台が見えない」）

Qシートの再生追従に `scrollIntoView` を使っていたのが原因。**scrollIntoView は
スクロール可能な祖先すべて（＝ページ全体）を動かす**ため、行が切り替わるたびに
ページが下へスクロールして上部の舞台ビューが隠れていた。
`#cuesheet-body`（overflow-yの箱）の `scrollTop` を直接計算する方式に変更。
検証: ページ最上部で9秒再生して `scrollY` が0のまま動かないことを確認。

### 8分を含む残り4曲のリズム点検結果

| 曲 | 判定 | 対応 |
|---|---|---|
| ちょうちょう | **正しい**（原曲は4分と2分のみ。8分がない） | 変更なし |
| ジングルベル | 8分ペアの平坦化はあるが骨格は保たれている | 現状維持（60BPMの成立性を優先） |
| ロンドン橋 | **付点（ソ・ラソファ）が均され、小節割りも原曲とずれていた** | 修正 |
| かえるの合唱 | 最終行が原曲と違う（下記） | 編曲として明記 |

### ロンドン橋の修正

冒頭の付点リズムを 1.5+0.5拍 で復元し、小節割りを原曲どおりに
（ソ・ラソファ｜ミファソ—｜レミファ—｜ミファソ—｜…｜レ—ソ—｜ミド—）。
チューニングで 96BPM→60BPM・滞空1.1。**パス58%→75%に改善**・リング8本・振り0・不可能0。

### かえるの合唱 — 原曲最終行は3人では物理的に不可能という結論

原曲の最終行「ケロケロケロケロ」は8分の ドドレレミミファファ ＝ **同音0.5拍ペア×4連続**。
実測（bpm 34〜72 × 滞空6種 × maxDup 2/3 の総当たり）で、3人・リング9本以内では
**どの組み合わせでも成立しない**。同音ペアはリング2本の交互で捌くとしても、
0.5拍間隔のキャッチ8連発に手が回らない。
→ 音符は現状のまま（最終行=冒頭句ドレミファミレドの再現で締める）とし、
  **descに「編成上の制約による意図的な編曲」であることを明記**した。
  ぶんぶんぶんのような量子化ミスではなく、「実演可能な振付だけを書く」というツールの
  目的に沿った判断。

## ループ再生の音ズレ修正（2026-08-23 本人報告「音ズレが発生しました」）

### 原因（設計バグ。初回実装から存在）

ループの巻き戻しは「前の周の音が終わる瞬間」を狙ったタイマーで `scheduleFrom` を
呼び直していたが、そこで **`playBase = ctx.currentTime + 0.08`＝「タイマーが発火した
時刻+80ms」を次の周の基準にしていた**。タイマーは音の終端ちょうどに発火するので、
**設計上、1周ごとに必ず80ms＋タイマー誤差の遅れが挟まる**。ループ稽古では周回のたびに
リズムがもたつき、「音ズレ」として聞こえる。

### 修正（js/main.js scheduleFrom / tick）

- 次の周の基準時刻を **`nextBase = playBase + (loopEnd − simT)`**（前の周の音声時刻の
  続きちょうど）と算術で決め、`scheduleFrom(loop.a*spb, nextBase)` で先行予約する。
  タイマーは境界より**0.35秒早く**起こす。タイマーが遅れても基準はずれない
  （遅れた分は最初の音が詰まるだけで、**周回誤差は蓄積しない**）。
- 継ぎ目では `beginSession()` を呼ばない。呼ぶと前の周の残響（ベルの減衰2.6秒）と、
  先行予約より後ろにある前の周の末尾の音が切れる。セッションの張り替えは新規再生時のみ。
- tick側: 先行予約直後は playBase が未来（境界時刻）を指すため、`pos < playT0` の間は
  1周分（loop.b−loop.a）を足して「前の周の続き」として表示する。これでプレイヘッドは
  境界で止まらず連続的に通過する。条件を `pos < playT0` にしたのは、再生中にループ開始
  より前へシークしたケース（playT0がループ外）を誤って巻き込まないため。

### 検証

`TOREI.audio.bell` をラップして**予約時刻そのもの**を記録（rAF停止環境でも計測できる方法）。
ぶんぶんぶん5小節目のループ（1周2.5秒）を11秒回し、同一音の予約間隔が
**1.875 / 0.625 秒の交互を4桁精度で完全一致**＝1周2.500秒ちょうどで刻むことを確認。
旧実装ではここに毎周+0.08秒が入っていた。

### 教訓

「今の時刻+マージン」を繰り返しの基準にすると、繰り返しのたびにマージンが蓄積する。
**周期的な予約は算術で決めた絶対時刻に連ね、タイマーは「その前に起きるための道具」に
格下げする**（タイマーの精度を信用しない設計は元からあったが、基準時刻の取り方が
それを台無しにしていた）。

## 出力遅延の補正（2026-08-23 本人報告「通常再生でも音ズレ」「一切音がしない」の調査）

### 実測した事実

公開版を新規読み込みして通常再生を計測: AudioContext running・32音を正しい時刻に予約・
エラー0件。**音声エンジン自体は健全**（ループ修正後の周回間隔も2.500秒ちょうどを維持）。

### 「通常再生でも音ズレ」への対応: outputLatency 補正

Bluetoothイヤホン・スピーカーでは音が耳に届くまで0.1〜0.3秒かかる（内蔵スピーカーでも
10〜30ms）。アプリの映像（プレイヘッド・舞台・Qシート追従）は音声クロックの「予約時刻」
基準で動いていたため、**補正しないと映像が音より先に動いて「ズレ」に見える**。
tick の表示位置から `ctx.outputLatency || ctx.baseLatency` を引き、
「いま耳に聞こえている音」の位置に表示を合わせた。
- ループの周回折り返し判定は補正後の pos で行うが、条件に `playT0 >= loop.a` を課して
  「ループ外から再生している」ケースを巻き込まない。
- 実測: この環境では latency 0.016秒（内蔵）。BT環境では0.1〜0.3が返り効果が大きい。

### 「一切音がしない」について

コード側の原因は見つからず、エンジンは実測で健全。環境要因の切り分けを本人に依頼
（→リロード・タブのミュート・出力先切替・他サイトの音）。Bluetooth接続の喪失は
「遅延（ズレ）→無音」という進行と符合する。

## 「持っている手でキャッチ＝両鳴り」の徹底（2026-08-23 本人指摘）

本人（実演者）指摘: 舞台ビューで「右手で持っているときに右手でキャッチ」が多発している。
これは物理的に**持っている方にも衝撃が加わって両方鳴る＝意図しない和音**なので基本なし。

### 調査で見つかった3層の問題

1. **検証ツールの幽霊（267件分）**: 厳密スキャンを書いたら274件検出されたが、大半は
   スキャン自身のバグ。①同時刻の in/out イベント処理順（out先だと幽霊保持が生える）
   ②浮動小数点の桁落ち（2.9000000000000004+0.7≠3.6。**invariants.mjsで踏んだのと
   同一の罠を再度踏んだ**）。両方直して真の違反は4件。
2. **スケジューラーの真の穴A**: 投げ準備の「逆の手へ退避（otherhand）」が、受け側の手の
   確定済みキャッチ予定と衝突しても通っていた。退避後の保持期間は将来の計画次第で
   確定できないため、「次の出番まで」近似も失敗（その出番を別の複製リングが担って
   退避リングが残留）。→ **受け手に以後の予定が1つでもあれば退避しない**（Infinity検査）。
3. **スケジューラーの真の穴B（本丸）**: 同一計画内の相互作用。「手1の持ちリングを
   手0へ退避 → 手1が投げる → **手0で受ける**」という計画が通っていた。退避チェックは
   既存予定しか見えず、キャッチ手選定（holdingAt）は適用前の退避を知らない。
   → **計画が otherhand 退避を含む場合、退避先の手をキャッチ候補から除外**。

### 結果

- `node tools/check_held_catch.mjs`（スキャンを恒久ツール化）: **全34曲で違反0件**。
- 回帰: 全曲 振り0・不可能0・和音20/20 維持。パス率もほぼ不変（きよし91→88%のみ）。
- 不変条件20シードも違反0件。

### 残課題（本人に提示する判断）

「キャッチ直前の手放し余裕」が0.3秒未満のキャッチが**77件**ある（<0.1秒: 27件）。
物理保持の重なりはゼロだが、0.08秒前に置いたばかりの手でキャッチする振付は実演では
事実上「持ったままキャッチ」。最低余裕マージン（例: 0.25秒）をスケジューラーに課すと
実演性が上がるが、成立性・パス率に影響するため全曲の再チューニングが要る。

## キャッチ前の回復時間 T_RECOVER（2026-08-24 本人指定）

前節「持っている手でキャッチ＝両鳴り」の残課題（手放し余裕0.3秒未満が77件）への対応。
**本人指定値: 投げてから0.4秒／脇に挟んでから0.3〜0.4秒。**

### 実装（js/scheduler.js・実質3行）

`T_RECOVER: 0.35` を追加し、**キャッチの占有区間を `[t − T_RECOVER, t + T_CATCH]`** とした。
- 共通値0.35で「投げ後0.50秒／脇後0.35秒」となり、指定の両方を満たす
  （投げは占有0.15秒・脇入れは0.7秒あり、その終わりから数えるため）。
  投げ側を指定の0.40より厳しくしても該当は41→45件と4件増えるだけで、実装を
  単純化できる利点が上回ると判断。
- ★**占有区間を前へ伸ばす方式にしたのが要点。** 片側（キャッチ計画時）だけ検査すると
  「先にキャッチを確定→後から投げを過去へ挿入」の順序が漏れる。busy の重なり判定は
  対称なので、どちらの順序でも守られる。

### 影響と再調整

制約なしの旧設定のままでは、3音が不可能・和音2箇所消失・平均パス75%→64%に劣化した
（曲の設定が「回復時間ゼロ」前提で限界まで詰められていたため）。11曲を再調整。

**★選好の設計を2度やり直した。** 最初は「パス率最大」にしたら聖者の行進が
116→44BPMになった（パス100%だが別の曲になる）。次に「最良の10pt以内で最速」に
したら浮動小数点の等値判定で候補を取りこぼした。最終的に
**「パス70%以上を満たす最速テンポ」**とし、聖者の行進は114BPM・パス86%に収まった。
テンポは曲の性格なので、十分なパス率が取れるなら速いほうを残す。

| 曲 | BPM | パス | 備考 |
|---|---|---|---|
| 聖者の行進 | 116→**114** | 69→**86%** | ほぼ据え置きで改善 |
| 疾風 | 187→**183** | 97% | 最速の看板を維持 |
| Swing Low | 99（据置） | 69→**97%** | 滞空のみ変更 |
| 五音の橋 | 97→**87** | 97% | 和音3/3を維持 |
| 草競馬 | 48→**46** | 91→86% | |
| オー・スザンナ | 91→**69** | 85→**91%** | テンポ低下が最大 |
| コロブチカ | 62→**46** | 63→74% | |
| 大きな古時計 | 60→**40** | 67→**88%** | 非表示曲 |
| グリーンスリーブス | 67→**41** | 14→27% | 非表示曲・元から限界例 |

結果: **全34曲で 不可0・振り0・和音20/20**、回復時間違反0件、不変条件20シード違反0件。

### 未対応（意図的）

`夕焼け小焼け`（非表示・4人）はリング17本で上限12本を超えており、回復時間の下では
4人・同音最大3でも成立解が無い（5人なら40BPM・パス94%）。**不可能音は0で演奏自体は
可能**なため、非表示曲であることも踏まえて手を入れていない。

### 事故とその教訓（同じ罠を2度踏んだ）

再調整の結果を `make_midi.py` へ反映する際、単純な正規表現
`"id": "<id>",.*?"bpm": (\d+).*?"flight": ([\d.]+)` を使ったところ、
**flight キーを持たない曲（susanna/gomon/kiyoshi/greensleeves は既定値1.2の暗黙指定）で
「次の曲の flight」を書き換えた**。蛍の光・重ね鈴・宵待ちが巻き添えになり、
重ね鈴のパスが70%→57%に落ちて発覚。
→ `"id"` の出現位置でエントリ境界を切り、その範囲内だけを置換する方式に修正
（flight キーが無ければ standTime の直後に挿入）。
**教訓: 生成元ファイルの一括置換では「そのキーが存在しない要素」を必ず想定する。**
非貪欲マッチは静かに次の要素へ越境する。差分を目視確認して気づけたのは、
`git diff` で変更行を一覧にしたため。

## ぶんぶんぶんのリング最適化とデフォルト反映（2026-08-24 本人編曲＋指摘への対応）

### 本人がブラウザ上で行った編曲（アプリの「曲を保存」JSONから取り込み）

- テンポ 48→65BPM
- 終止部（16〜18拍）に三度下のハモリを3音追加し、保持キャッチ和音で締める構成に

この状態をデフォルト（`tools/make_midi.py` のソース）へ反映してほしいとの依頼。
反映前に実測したところ、65BPM・滞空1.4のままだと**1音不可能・和音1/3不成立**だった。

### 本人指摘「演者3の最初のファが1回しか使われていない」

上記の実測中、演者3のホームリング（ファ②）が全曲中**1回しか投げられていない**
無駄を発見。さらにレ②は**0回**（一度も使われない幽霊リング）だった。
「リングの数を減らせるとなお良い」との要望も合わせて対応。

### 解決: maxDup=1（複製リングを作らない）

bpm×flight×maxDupの総当たりで「不可0・和音3/3を満たしつつ全リングの最小使用回数を
最大化する」設定を探索。**54BPM・滞空0.7・maxDup=1**が、無駄なリングをゼロにできる
最速のテンポだった（56BPM以上ではmaxDup=1で成立する設定が存在しない＝どこかで
複製が必須になる）。

結果、**リング5本＝音高数と同数**（ミ・レ・ファ・ソ・ド各1本）になり、
全リングが最低3回以上使われる（演者3のファは1回→**8回**に）。パスは0%→47%。

### スキーマ拡張が必要だった

`maxDup` は曲プリセットの一部として保存する仕組みが無かった（`make_songs_js()` の
出力にも `loadPreset()` の反映処理にも項目が存在しなかった）。このままではぶんぶんぶんを
選んでもUIの既定値2が使われ、8本構成に戻ってしまう。
- `make_songs_js()`: `maxDup` を**曲が明示的に指定したときだけ**出力する任意項目に
  （既存33曲は無指定のまま＝既定2で不変）。
- `js/main.js` `loadPreset()`: `p.maxDup` があれば `state.cfg.maxDup` と
  `#inp-dup` へ反映。他の推奨設定（flight/passMode/standTime）と同じ「曲指定が
  無ければ直前の値を維持する」流儀に揃えた。

検証: ぶんぶんぶん選択でリング5本・maxDup=1が自動適用。別の曲（聖者の行進）へ
切り替えても壊れない（maxDup=1を引き継いでも警告なしで動作。他33曲は無指定なので
本来は既定2のままのはずが、直前値を引き継ぐ既存仕様どおり1のままになるのは
意図通り＝ユーザーが手動で戻すまで前の曲の値が残るのは他の設定項目と同じ挙動）。
全34曲で振り0・不可能0・和音23/23（100%）、回復時間・不変条件とも違反0件。

## 準備フェーズの再設計：手より先に脇を使う（2026-08-24 本人指摘の深掘り）

前節でぶんぶんぶんのリングを5本に最適化した際、演者3以外の演者（各2本持ち）は
**開演前に両手を無条件に埋めてしまい**、曲頭で誰ともパスできない状態を作っていた。
本人指摘「最初のソファミは他人へのパスで成り立たせられるはず。システム的な
考慮エラーでは」を受けて調査し、実際に**スケジューラーの設計漏れ**だったことを確認した。

### 発見した根本原因

`preassign()`（開演前の準備）は、演者が持つ全リングを**配列順で「手2本→脇」**という
機械的な順序で割り当てていた。**「このリングは曲のどの時点で初めて必要か」を
一切見ていなかった**ため、後半でしか使わないリングも問答無用で開演前から
両手を塞いでいた。脇（取り出し0.7秒）という選択肢があるにもかかわらず、
2本しか持たない演者では一度も使われていなかった。

### 修正: 初出時刻の早い順に「手→脇」を割り当てる

- 演者の持ちリングを、そのピッチが曲中で**最初に必要になる時刻**の早い順に並べ替える。
- **最も早く必要な1本だけを手に**（即応性が要るため）、残りは**脇の枠が空いている限り脇へ**。
  脇が尽きたときだけ2本目の手を使う（＝旧実装と同じ最終手段）。
- ★実装の落とし穴（1回踏んだ）: 脇へ運ぶ「作業する手」の割り当てを素朴に実装したところ、
  演者が3本以上持つ曲（脇1枠のみ）で**手の恒久保持と一時使用が時系列で重なるバグ**が
  発生し、20シードで500件の不変条件違反を出した。**脇へ運ぶ作業は常に手0だけを使う**
  よう固定して解決（手0の恒久保持はこの並べ替えの中で必ず最初に処理され＝この演者の
  準備アクションの中で最も遅い時刻に始まるため、後から処理される＝より早い時刻に
  起こる脇作業と絶対に重ならない、という時系列の性質を利用）。

### 効果（ぶんぶんぶんで確認）

| | 修正前 | 修正後 |
|---|---|---|
| 冒頭ソ（note0） | 自己キャッチ | **演者1→演者2 パス** |
| 冒頭ファ（note1） | 自己キャッチ | 自己キャッチ（演者3唯一の持ちリングのため不可避） |
| 冒頭ミ（note2） | 演者1→演者3 パス | **演者1→演者3 パス（維持）** |
| パス率 | 47% | **59%** |

note1だけ自己キャッチが残るのは、演者3が1本しかリングを持たないヨナ抜き5音・3人編成の
構造上の限界（渡す相手がいてもリング自体を増やさない限り解消しない）で、修正漏れではない。

### 全曲への影響と再調整

前回のT_RECOVER導入と同じパターンで、3曲（重ね鈴・ぶんぶんぶん・ジングルベル）が
新しい準備フェーズの下で不成立になり、bpm/flightの再チューニングが必要だった。

| 曲 | BPM | 滞空 | 結果 |
|---|---|---|---|
| 重ね鈴 | 66→60 | 1.3→0.6 | 和音5/6→**6/6**、パス70→80% |
| ぶんぶんぶん | 54（据置） | 0.7→1.0 | 和音2/3→**3/3**、パス38→59% |
| ジングルベル | 60→50 | →0.75 | 不可1→**0** |

### maxDupの引き継ぎ事故と修正

再調整・実機確認の過程で、**11曲が壊れる別の事故**を発見した。ぶんぶんぶんだけに
`maxDup:1`を持たせ「無指定なら前の曲の値を引き継ぐ」（flight等と同じ流儀）にしていたら、
**ぶんぶんぶんを選んだ後に他の曲へ切り替えると、その曲が本来必要とするmaxDup=2に
戻らず再生不可能になる**事故が実機で発生した（twinkle, swinglow, hotaru, gomon,
yoimachi, korobeiniki, kasanesuzu, sakura, chocho, birthday, frereの11曲）。
flight等の「引き継いでも大抵は問題ない」設定と違い、**maxDupは値の違いが
再生可否そのものを左右する**ため、同じ流儀を適用したのが誤りだった。
→ 全34曲へ検証済みの値（既定2、ぶんぶんぶんのみ1）を**常に明示**する方式に変更
（`make_songs_js()`が無条件出力・`loadPreset()`が無条件反映）。

### 検証

全34曲を実機で連続選択して警告0件（サーバー再起動後の新規セッションで確認）。
`node tools/analyze.mjs`（振り0・不可能0・和音23/23）、`check_held_catch.mjs`
（両鳴り0・回復時間違反0）、`invariants.mjs`（20シード違反0）すべて通過。

## フレーズ末の判定を拍子基準に修正（2026-08-24 本人指摘）

本人がブラウザ上でぶんぶんぶんのテンポを上げたところ、「はちがとぶ」の結びのドに
「**フレーズ末ではないので振りでの代用は使えません**」という警告が出た。
本人指摘「これ最初のドは手で振ることで鳴らせると思います」は音楽的に正しく、
**フレーズ末判定のバグ**だった。

### 原因: 「3拍以上」という絶対値が拍子を無視していた

2026-08-23 に導入した判定は
`次の打点までの休み >= 中央値の2倍 かつ >= 3拍` だったが、この「3拍」が固定値で、
**2/4拍子のぶんぶんぶんでは「まるまる1小節の休符」（2拍）ですらフレーズ末と
認められなかった**。ドの後には2拍＝1小節分の休みがあり、音楽的には明白なフレーズ末。

### 修正: 「3拍固定」→「beatsPerBar（1小節分）」

全34曲で4つの定義を比較検証した結果、1小節分が最適だった。

| 定義 | フレーズ末の合計 | 評価 |
|---|---|---|
| A: 3拍固定（旧） | 101箇所 | 2/4拍子を取りこぼす |
| **B: 1小節分（採用）** | **94箇所** | ぶんぶんぶん1→4箇所。全体はむしろ厳格に |
| C: 1.5秒以上 | 189箇所 | 緩すぎ（蛍の光が21箇所） |
| D: 1小節 or 1.5秒 | 189箇所 | 同上 |

**採用案は現行より厳格になる**のが要点。拍子に合わせたことで音楽的な区切りと一致し、
ぶんぶんぶんのような取りこぼしを救いつつ、蛍の光の過剰検出（21→2箇所）も同時に直る。

### 検証

- ぶんぶんぶんを60BPMにすると、当該のドが
  「どの演者も間に合いません」→「**演者2がフレーズ末で振って鳴らします（1/1本目）**」
  に変わることを実機で確認（＝演奏不可能だった箇所に解決策が提示されるようになった）。
- 収録34曲は既定設定で余裕を持って成立しているため影響なし
  （振り0・不可能0・和音23/23を維持）。判定が緩んでも振りは発生しない。
- `check_held_catch.mjs`・`invariants.mjs` とも違反0件。実機で全30曲巡回して警告0件。

### 教訓

**音楽的な量を「拍数の絶対値」で表すと拍子で破綻する。** 3拍は4/4では
「小節の大半」だが2/4では「1小節半」で、意味が変わる。小節（beatsPerBar）や
音価に対する相対量で表すこと。同じ罠は「同音の再出現間隔2.4秒」等にも潜在的にある
（こちらは秒数なのでテンポには追随するが、拍子とは無関係なので現状は問題ない）。

## ぶんぶんぶん60BPM確定と、全曲フレーズ末の点検（2026-08-25）

### ぶんぶんぶんを60BPMへ（本人指定）

前節の修正で「はちがとぶ」の結びのドが振りで救済できるようになったのを受け、
本人がテンポ60BPMを選択。60BPMでの選択肢を提示し、
**リング5本・振り1回を許容**する構成を本人が採用した。

| 選択肢 | リング | 和音 | 振り | パス |
|---|---|---|---|---|
| **採用: 滞空1.0** | **5本** | 2/3 | **1回（ドを手で振る）** | 31% |
| 滞空1.1以上 | 5本 | 2/3 | 0回 | 33% |
| maxDup=2 | 8本 | 3/3 | 0回 | 28% |
| （54BPMのまま） | 5本 | 3/3 | 0回 | 59% |

60BPMではどの構成でも和音が3/3にならない（終止のミ+ドが分散する）ため、
**ご指摘のドを実際に「手で振って鳴らす」演出として見せる**方を優先した形。

### 全曲のフレーズ末点検 → 弱起の曲を取りこぼしていた

「1小節分」の基準で全34曲を点検したところ、**2つの傾向**が見つかった。

- **[A] 宵待ち19件**: 全音符が3拍間隔の均等な曲で「休み」自体が存在しない。
  中央値x2条件で落ちるのは**正しい挙動**（休符のない曲にフレーズ末は最終音のみ）。
- **[B] 14件**: 4拍子で休み3拍（＝1小節の3/4）が惜しく届かない。楽譜を確認すると
  **すべて明確なフレーズ末だった**:
  メリーさんのひつじ13拍（「…ひつじ**ね**」の結び）、
  グーチョキパー21/29拍（輪唱の各行末「…なにつくろう」）、蛍の光27拍、
  大きな古時計、フレール・ジャック。
  **弱起（アウフタクト）の曲**は2拍目などからフレーズが始まるので、
  結びの音の後ろは「小節の残り」＝1小節未満の休符になる。

### 修正: 「次の打点が小節頭」なら1小節未満でもフレーズ末

条件を `休み >= 中央値x2 かつ（休み >= 1小節分 または（次が小節頭 かつ 休み >= 中央値x3））`
に拡張。★「次が小節頭」だけでは**単に小節をまたぐ通常の音符まで拾ってしまい**、
全34曲で94→177箇所とほぼ倍に緩む（アメイジング・グレイスが2→14）。
**休みの絶対量（中央値x3）を併せて課す**ことで101箇所に収まり、狙った箇所だけを拾う。

| 定義 | 合計 | 評価 |
|---|---|---|
| 1小節分のみ | 94 | 弱起の曲を取りこぼす |
| 小節頭のみ追加 | 177 | 緩すぎ |
| 小節頭 かつ 中央値x3/4小節 | 105 | アメイジングが2→6と過剰 |
| **小節頭 かつ 中央値x3（採用）** | **101** | 狙った箇所だけ |

検証: メリーさんのひつじ13拍・グーチョキパー21/29拍・大きな古時計21/45/73拍が
新たに認識され、アメイジング・グレイスは2箇所のまま維持。全34曲で不可能0・
両鳴り0・回復時間違反0・20シード不変条件違反0。実機で全30曲巡回し演奏不可能な曲なし。

### 教訓（前節の続き）

拍子を考慮しても**「小節の頭から始まるフレーズ」を暗黙に仮定していた**。
弱起は音楽では普遍的なので、小節内の位置に依存する判定を書くときは
「フレーズが小節頭から始まるとは限らない」を常に疑うこと。

## 演者1人分の動きモーダル（2026-08-25 本人要望）

Qシートの表は「いつ何をするか」は分かるが、**体の動きは想像するしかない**。
本人要望「ホーム画面の一番上（舞台ビュー）の1人分のような形で、学習できるように
見られるようにしたい。モーダルで」を実装した。

### 設計: 舞台ビューを「別実装」せず、変換で使い回す

`js/stage.js` の `render()` を「canvas準備」と「描画本体（`drawScene`）」に分離し、
`renderSolo(canvas, t, perfId, w, h)` を追加。**対象演者を中心に平行移動＋拡大する
座標変換を掛けて、既存の描画ロジックをそのまま呼ぶ**。
1人分を別実装すると本番の舞台ビューと必ず見た目がズレるため、同じコードを通す。

- ソロ表示では対象演者以外の演者・スタンド・脇ラベルを描かない。
- 背景の大窓は拡大で画面外になるので省き、床は対象演者の周りだけ。

### ★owner ではなく「描画位置」で絞る（実装中に踏んだ罠）

最初、リングの絞り込みを `ring.owner === perfFilter` で書いたところ、
**無関係なリングが画面の端に浮いて表示された**。原因は
**`ring.owner` が「曲の最終的な持ち主」を指す静的な値**で、パスで持ち主が移った後の
その時刻の所在を表さないこと（実測: ぶんぶんぶんの ring3「ソ」は owner=0 だが
曲頭で演者2へパス済み）。
→ **実際に描画される座標が対象演者の視野（±125px）に入っているかだけで判定**する方式に
統一した。手・脇・スタンド・飛球のすべてを一貫して扱える。
同じ `owner` 依存が「脇ラベル」と「腕の描画」にもあり、後者は
**パスで受け取ったリングへ腕が伸びない**潜在バグだったので併せて修正した。

### モーダルの作り

- Qシートの演者タブ横に「▶ この演者の動きを見る」ボタン。
- **本編の再生とは独立した時計**で動く（本編を止めずに確認できる）。
  既定0.5倍速、0.25〜1倍まで可変。
- 「前の動作 / 次の動作」でQシートの行を1つずつ送る。所作を1コマずつ確認できる。
- 下に「いま何をしているか」を1行表示（高さ固定で、切り替わっても画面が揺れない）。
- Esc・背景クリック・閉じるボタンで閉じる。モーダル表示中は本編のキー操作
  （Space=再生、矢印=シーク）を奪わず、モーダル側の操作に割り当てる。
- **`recompute()` で開いていたら閉じる**。曲切替・編集でスケジュールが別物になるので、
  古い動作列のまま操作させない。

### canvasの寸法（既出の罠の再来）

`#stage` で踏んだのと同じ問題に再び当たった。**canvas自身の `clientWidth/Height` は
`setupCanvas` が書いたインラインstyle（＝前回の寸法）に引きずられ、モーダルを開いた
直後は 0 になることもある**。
→ 幅は親の内容幅（`clientWidth − 左右padding`）、高さは `#solo-panel` の
CSSカスタムプロパティ `--solo-h` から読む（`#stage` の `--stage-h` と同じ方式）。
`box-sizing:border-box` なので枠線分を引くと逆に隙間ができる点にも注意。

### 検証

全30曲でボタンが出てモーダルが開き、内容が空でないことを確認。「白紙から作る」だけは
動作が無いので通知を出して開かない（意図どおり）。ステップ送りで投げ・キャッチ・
脇の出し入れ・パス受けが順に追えること、Esc／曲切替で閉じること、
モバイル375pxで全操作が44px・パネルが画面内に収まることを確認。
本編の舞台ビューは変更後も正常（`owner`→位置の修正を含む）。
スケジューラーには影響なし（analyze/check_held_catch/invariants すべて従来どおり）。

## 演者間パス率の常時表示（2026-08-25 本人要望）

本人「ぶんぶんぶんでまた演者間パスの数が減ったと思います。20%以上にしたいので
数値化も表示できるようにしたい」。

### 表示

`#ring-summary` の先頭に「演者間パス **31%**（10/32投）」を常時表示。
目安（`PASS_TARGET = 20`%）を下回ると警告色（リング超過と同じ扱い）。
自分で投げて自分で受ける分は含まない＝ジャグリングの見どころの割合をそのまま出す。

### 「減った」の実態: モーダル追加は無関係、60BPM化のとき下がっていた

コミットを遡って実測したところ、**モーダル追加（fb7eb72）の前後で31%のまま変化なし**。
下がったのは 54BPM→60BPM にしたとき（**59% → 31%**）で、その時点では
「和音3/3が取れない」ことだけを比較表に出しており、**パス率の低下を明示していなかった**。
今回の常時表示で、この種の見落としは起きなくなる。

### 60BPMではパス率33%が上限（構造的な理由）

60BPM固定で maxDup×滞空を総当たりしても31〜33%が上限。
**リング5本（音高5種と同数）だと各演者が「自分の担当音」に固定される**ため、
構造的にパスが増えにくい。パス率を大きく上げるにはリングを増やすかテンポを落とす必要がある。

テンポ別の最良（不可0が条件）:

| BPM | 最良パス | 構成 |
|---|---|---|
| 60 | 33% | dup=1 滞空1.1 和音2/3 リング5 |
| 58 | 39% | dup=1 滞空0.6 和音3/3 振り1 リング5 |
| 56 | 56% | dup=2 滞空0.9 和音3/3 リング8 |
| 54 | 59% | dup=1 滞空1.0 和音3/3 リング5 |
| 50 | 91% | dup=2 滞空0.6 和音3/3 リング6 |
| **48** | **97%** | **dup=2 滞空1.0 和音3/3 振り0 リング6** |

48BPMまで落とすと「リング6本・パス97%・和音3/3・振り0」という理想的な構成が現れる。
テンポとパス率のどちらを取るかは本人判断。

## 音声タイムラグ再発の点検と堅牢化（2026-08-25 本人報告）

### 実測（前回と同じ手法で総点検）

- 通常再生: 35音すべて楽譜どおりの間隔で予約（60BPM: 1.000 / 0.500秒刻みが正確）
- ループ: 周回ギャップすべて1.0000秒ちょうど（前回修正の退行なし）
- outputLatency 0.016秒・コンソールエラー0件
- モーダル追加(fb7eb72)・パス率表示(4d641d4)の前後でスケジュールに変化なし

→ **エンジンは健全。コード側での再現は取れず**。ただし点検で堅牢性の穴を2つ発見し修正。

### 修正1: outputLatency 補正のクランプ

前回入れた出力遅延補正 `lat = ctx.outputLatency || baseLatency` に**上限がなかった**。
Chromeには outputLatency が異常な巨大値を返し続ける既知の不具合がある
（Bluetooth機器の切替後など）。異常値が来ると:
- 映像が音から数秒遅れて見える（＝「音声タイムラグ」の見え方そのもの）
- 停止→再開のたびにその分だけ大きく巻き戻る（state.pos が補正後の値のため）
→ **0.5秒でクランプし、異常時は baseLatency（小さく安定）へフォールバック**。
ユーザー環境で再発した場合の最有力なコード側要因として先回りで塞いだ。

### 修正2: ループ開始直後のプレイヘッド誤ワープ

周回ラップ表示（先行予約ウィンドウ中に前の周の続きを表示する仕組み）の条件が、
**フレッシュ開始直後にも誤発火**していた。開始マージン0.08秒＋出力遅延の間は
pos < ループ開始 となり、ラップ条件（pos < La かつ playT0 >= La）を満たしてしまう。
→ ループ再生を開始した瞬間、0.1〜0.3秒だけプレイヘッドがループ終端へ飛ぶ。
`loopChained` フラグ（直近の scheduleFrom が継ぎ目か）を導入し、
**継ぎ目のときだけラップ**するようにした。

### ユーザー環境での切り分け（コード側で説明がつかない場合）

Bluetooth接続の劣化が前回と同じく有力。リロード／出力先を内蔵スピーカーへ切替／
BT機器の再接続で解消するかを確認してもらう。

## 楽曲・MIDI（2026-08-21 追加）

本人方針: **振り（手元で鳴らす）はジャグリング要素がなく好まれない** → 楽曲選定基準は
「全音符が投げで成立すること」。隣接同音を避け、同音再出現は1.2秒以上、遅いテンポ=高い投げ。

- `tools/make_midi.py` が**単一ソース**。実行すると `midi/*.mid` と `js/songs.js`
  （シミュレーターのプリセット）を両方生成する。楽曲の追加・修正はこのファイルだけ触る。
### ★編成が成立する条件（2026-08-21 実測。設計の中核）

スタンド操作は体ごと動くため**曲中はほぼ不可能**。したがって
**全リングを開演前にスタンドから手・脇へ配っておく**必要がある（`preassign()` が自動で行う）。
1人が持てるのは **手2本 + 脇1本 = 3本**。ここから2つの帰結が出る:

1. **音高数 ≦ 人数 × 3** が絶対条件。
2. **音高数が少ないほどパッシングが増える**。手元に余裕があるほどリングを他人へ回せるため。
   実測: 音高5(ペンタトニック) → パス率85〜91% ／ 音高8〜9 → 57〜63% ／ 音高10 → 8%。
   **ジャグリング要素を最大化したいならペンタトニック（5音）。**

さらに、音符が詰まった曲は1人あたりのキャッチ間隔が
「キャッチ0.25 + 投げ0.15 + 滞空1.2 ≒ 1.6秒」を下回ると破綻する。
→ ゆったりした音符ほど高い投げができ、ジャグリングとしても映える。

### ★編成は3人が基準（2026-08-21 本人指定「基本的には4人はないパターン」）

収録18曲のうち**16曲が3人編成**。曲を選ぶと推奨設定（人数・テンポ・パス設定・スタンド時間）が
自動適用され、すべて「振り・不可能ゼロ」を検証済み（脇最大1本／スタンド操作中の同時実行違反0）:

**本人方針（2026-08-21）: 基本は既存曲（実在の曲）。オリジナルは参考として残す。**
ペンタトニックである必要はない。全18曲、すべて「振り・不可能ゼロ」を検証済み。

#### 既存曲（12曲）— こちらが主軸

| 曲 | 人数 | テンポ | 滞空 | 音高 | リング | パス率 |
|---|---|---|---|---|---|---|
| 聖者の行進 | 3人 | **116** | 0.75 | 5 | 6 | 66% |
| Swing Low, Sweet Chariot | 3人 | 99 | 0.9 | 5 | 6 | 68% |
| メリーさんのひつじ | 3人 | 96 | 1.0 | 4 | 6 | 60% |
| よろこびの歌（第九） | 3人 | 96 | 1.0 | 5 | 6 | 62% |
| オー・スザンナ | 3人 | 91 | 1.2 | 6 | 7 | **85%** |
| アメイジング・グレイス | 3人 | 90 | 0.85 | 5 | 7 | 42% |
| チューリップ | 3人 | 84 | 1.2 | 4 | 5 | 65% |
| 蛍の光 | 3人 | 71 | 0.8 | 5 | 8 | 71% |
| コロブチカ（テトリスのテーマ） | 3人 | 62 | 1.1 | 8 | 10 | 60% |
| きらきら星 | 3人 | 36 | 0.8 | 6 | 6 | 64% |
| きよしこの夜 ※4人以上 | 4人 | 54 | 1.2 | 9 | 11 | 74% |
| グリーンスリーブス ※4人以上 | 4人 | 67(8分) | 1.2 | 10 | 13 | 13% |

#### オリジナル曲（6曲）— 参考・限界検証用

| 曲 | テンポ | 滞空 | 音高 | リング | パス率 |
|---|---|---|---|---|---|
| 疾風 | 187 | 0.8 | 5 | 5 | 51% |
| 投鈴ワルツ | 160 | 0.7 | 4 | 4 | 45% |
| 四つ辻 | 154 | 0.75 | 4 | 5 | 61% |
| 宵待ち | 114 | 0.75 | 5 | 7 | 74% |
| 三つ巴 | 97 | 1.0 | 3 | 3 | **100%** |
| 五音の橋 | 97 | 1.2 | 5 | 5 | **97%** |

既存曲の設定は「**パス率60%以上を保てる最速**」で選んでいる。
さらにパスが欲しい場合はテンポを落とす（第九48BPMでパス100%、
メリーさん72BPMで90%、アメイジング48BPMで85%）。

### ★スタンド操作は2秒（2026-08-21 本人指定で4秒から変更）

4秒だとテンポの制約が大きく、2秒に短縮したことで全曲が速くなった
（疾風150→187、宵待ち71→114、Swing Low 70→112、スザンナ66→92BPM）。
パス率もほぼ全曲で改善している（きよし63→74%、テトリス71→60%は速度と引き換え）。

### ★スタンドを使う曲・使わない曲（2026-08-21 本人質問への回答）

**曲中にスタンドを使うかどうかは、リング数が手元容量（人数×3）に収まるかで決まる。**
スタンド所要時間は関係ない（2秒に縮めても、リングが手元に収まる曲は0回のまま）。

- リング3〜6本（手元上限9本に余裕）→ 開演前に全部配れるので**曲中の台操作は0回**
- リング7本以上（上限に近い/超える）→ 曲中も往復が必要（テトリス5回、きよし6回）

つまり**「パスを増やす」と「スタンドを使う」は相反する**。リングが少ないほど手が空いて
パスが増えるが、同時にスタンドが不要になる。スタンドを演出に使いたい場合は
音高を増やす／曲中に4秒級の「間」を作る／中盤で音階セットを丸ごと交換する、などの設計が要る。

### ★★速さ・投げの高さ・パス率のトレードオフ（2026-08-21 実測）

本人指摘「全体的に遅すぎて曲っぽくない。1.5倍程度は速く」を受けて全曲を見直した。
テンポの上限を決めていたのは**滞空時間**だった。

- **1リングを投げ直すサイクル = キャッチ0.25 + 投げ0.15 + 滞空**。滞空1.2秒なら1.6秒かかる。
  同音がこれより短い間隔で再出現すると複製リングが必要になり、手元が埋まってパスが減る。
- したがって**速い曲は滞空を短くする**（＝低く速い投げ）。滞空0.8秒ならサイクル1.2秒まで縮み、
  147〜160BPMが可能になる。投げの高さは頭上0.6〜0.9mで、パッシングとしてはむしろ自然。
- 遅い曲は滞空1.2秒（頭上1.8m）の高く大きな投げが映える。**曲ごとに使い分ける**。
- 必要な音高数の目安: **N ≧ (0.4 + 滞空) × BPM / 60**。
  例: 150BPM・滞空0.85 → N ≧ 3.1（音高4以上）／97BPM・滞空1.2 → N ≧ 2.6（音高3で足りる）
- 曲データに `flight` を持たせ、曲を選ぶと滞空も自動で切り替わる。

**3人に収まらないのは全音階（7音）の曲**。きよしこの夜とグリーンスリーブスは
ペンタトニック化できないため3人だと極端に遅くなる（34BPM／成立せず）。
既存曲を3人向けにするには次の2手を使う（`make_midi.py` に実装済み）:

1. `merge_repeats()` — 隣接する同音をタイに統合する（投げ直しが物理的に不可能なため）
2. **音域を畳む** — 同じ音名の別オクターブを1つにまとめて音高数を減らす。
   例: アメイジング・グレイスは高いソ(G5)を低いソ(G4)へ畳んで音高6→5にし、
   3人29BPM(パス65%)から3人62BPM(パス61%)へ改善した。

### ★★パス率を決めるのは「リング数と人数の比」（2026-08-21 実測）

**リング数 ≦ 人数 なら全員が常に片手で余裕を持て、パス率はほぼ100%になる。**
リングが増えて手元が埋まるほど、受け手の手が塞がってパスできなくなる。

| リング数 ÷ 人数 | パス率の目安 | 例 |
|---|---|---|
| 1.0 以下 | 95〜100% | 三つ巴(3/3)、四つ辻(4/3) |
| 1.2〜1.5 | 60〜90% | 宵待ち(5/4)、蛍の光(5/3) |
| 2.5 以上 | 10%以下 | グリーンスリーブス(13/4) |

リング数 = 音高数 + 複製数。**複製は「同音の再出現間隔 < キャッチ+投げ+滞空(約1.6秒)」で発生**
するので、同音間隔を広く取れば複製ゼロにできる。曲作りでは
`cycle_phrases()`（tools/make_midi.py）が同音間隔を検査しながら旋律を組む。

### ★3人編成で作るための条件（2026-08-21 実測）

**音高3〜4つに絞り、同音間隔を「音高数×1拍」で厳密に巡回させる。**
これでリング数が音高数と一致し（複製ゼロ）、3人の手元に余裕ができてパスが最大化する。
テンポも上げられる（四つ辻は92BPM、三つ巴は84BPM）。

- 音高3つ（ド ミ ソ）= リング3本 = 3人が1本ずつ → **パス100%**。3人の間でリングが
  絶えず入れ替わる、ジャグリングとして最も見応えのある編成。
- 音高4つ（ド ミ ソ ラ）= **パス97%**。旋律に少し表情が出る。
- 音高5つ（ペンタトニック）= 複製が入りやすく3人ではパス67%程度（蛍の光）。
- 既存曲を3人でやると極端に遅いテンポが必要（きよし=35BPM、アメイジング=29BPM）。

単調にならない工夫: **フレーズの切れ目に休符を入れて巡回順を切り替える**
（上行→下行→変形）。休符を挟まないと切り替え時に同音間隔が崩れて複製が発生する。

既存曲の3人設定（参考値・いずれも検証済み）:
五音の橋 48BPM(パス70%) ／ 宵待ち 37BPM(パス65%) ／ アメイジング 29BPM(パス65%)。

- **隣接する同音は投げ直せない**（キャッチ+投げ+滞空 ≒1.6秒が必要）。既存曲を編曲するときは
  `make_midi.py` の `merge_repeats()` で自動的にタイへ統合する。蛍の光の "G G G" のような
  同音連続はこれで処理している。

- スタンド時間を3秒以下に設定できるなら、3人でも宵待ちが88BPMで成立（パス78%）。
  （＝現実の演技でも、スタンドが手の届く位置にあることが重要）。
- コロブチカは原曲の8分音符が連続して投げが間に合わないため、**音価を2倍に引き伸ばした投鈴版**。
  旋律はロシア民謡でパブリックドメイン（pianoletternotes.blogspot.com の音列で照合）。

- `make_midi.py` は生成時に **同音間隔と使用音高数を自動検証**して警告を出す
  （`check_song()`。同音間隔2.4秒未満を「短い」と判定）。曲を足すときはこの警告を見る。
- 出典・編曲注記は make_midi.py の docstring 参照。

## パッシング対応（2026-08-21 本人要望「パッシング要素をもっと」）

- 投げ手とキャッチ手を完全分離。**別の演者へのパス**が可能（キャッチした演者にリングの所有が移る）。
- UI「パッシング」= 多め（既定）／自然に／なし。多め設定ではパスがコスト優遇され、
  カノンで39投中32がパスになるなど、演者間をリングが行き交う編成になる。
- 舞台ビューでは舞台を横切る大きな放物線、行動表では演者バンドをまたぐ弧として表示。
- 行動表テキストにも「◯◯へパス」と明記。

## スタンド持ち替え時間（2026-08-21 本人要望で2倍に）

- 既定4.0秒（UIで1〜8秒に調整可）。旧2.0秒。
- 4秒化に伴いスケジューラーへ「段取り」を追加。3経路を**すべて評価して最善を選ぶ**
  （最初に見つかった経路で確定すると良い経路を取り逃すことが実測で判明）:
  1. **直前取得**（jit）
  2. **早取り**（hold）: スタンドから早めに取って手に持って待つ（最大25秒先行）
  3. **補充**（restock）: 暇な時間に 台→手→脇 を済ませ、直前は脇からサッと取る
- 手が塞がっている時の逃がし先は「逆の手へ持ち替え(0.4秒) → 脇 → スタンド」の順で選ぶ。
- スタンドへ戻すのは「次の使用まで スタンド時間×2＋8秒 以上」の時だけ。
- 再生は準備が長くても-4秒から開始（それ以前は取得・待機のみで投げは含まれないため）。

## 脇は1人1本（2026-08-21 本人指定）

- UI既定 1本。**占有は個数ではなく時間区間で管理**する（`wakiIv` と `wakiHasRoom`）。
  個数カウント方式だと、処理順と時刻順のズレで過去の時刻への挿入を見逃し、
  実際には3本挟まった状態が生成される（実際に発生したバグ）。
- 検証は「store(waki)→pickup(waki) の重なり最大数」を数えれば確認できる（全曲1で確認済み）。

## 演技テクニック（本人からの情報）

- **保持キャッチ和音**: リングを1本持った手で別のリングをキャッチすると両方鳴って和音になる。
  → **2026-08-22 スケジューラー対応済み**（詳細は次節）。1人の手で2音出せるため、
  音高数に対して演者が足りない曲でも成立させやすくなる。

## 保持キャッチ和音のスケジューラー対応（2026-08-22 実装）

同時刻の2音（和音）を、2つの手ではなく**1つの手**で成立させる技法。`js/scheduler.js` に追加。

- **仕組み**: 片手が静かに持っているリングA（`to:Infinity`の保持）がある状態で、
  別のリングBがその同じ手にキャッチされると、両方鳴って和音になる。キャッチ直後、
  その手は必ず1本に戻す（Aを逆の手／脇／スタンドへ逃がす）——2本を持ったままにすると
  「その手が今どのリングを持っているか」というスケジューラー全体の前提
  （`possConflict`）が壊れるため。**この分離経路が見つからない場合は和音そのものを不採用**にし、
  従来通り2つの手（＝2人分、またはパスで別演者）に振り分ける。既存の動作を壊さない
  純粋な追加機能（見つからなければ何も変わらない）。
- **既存曲は無改造で恩恵を受ける**: 収録18曲中、作曲時に既に「同時刻の複数音」（終止和音など）を
  持っていた9曲（聖者の行進・四つ辻・Swing Low・蛍の光・五音の橋・宵待ち・コロブチカ・
  アメイジング・グレイス・グリーンスリーブス）は、**全曲で少なくとも1箇所、この技法が
  自動的に選ばれるようになった**（以前は2人・2手で処理していた箇所が1手で成立するようになった）。
  全18曲で振り・不可能ゼロを再検証済み（1曲あたり100シード×18曲=1800通りを直接スキャンし、
  脇本数超過・時間重複・リング参照の破損のいずれも0件）。
- **乱択リスタートとの統合**: 和音を「試す/試さない」自体もseedごとに確率的に揺らす
  （seed 0は必ず試す＝決定論的な基準、seed>0は9割の確率で試す）。和音を使うと成立しない
  レアケースでも、20シードの中に和音なしの経路が残るようにするため。
- **実装で踏んだ落とし穴（2件、いずれも実装直後にNode検証で発見・修正）**:
  1. **リングIDの0を真偽値として判定するとバグる**（`chordRing: 0` は `!!0 === false`）。
     `plan.chordRing` の判定は必ず `!= null` を使うこと（`!!plan.chordRing` は禁止）。
     この不具合により「和音成立後の自動パーキング」が誤って走り、脇の1本枠を
     二重予約する実害があった（`hotaru` で発見）。
  2. **複製リング（ghost, id:-1）を実体化する処理**（`newRing()`）が
     `applyChordPlan` に抜けていた（`scheduleNoteIndependently` にはある）。
     和音の「新しく投げる側」が複製リングだった場合にリング参照が壊れる経路。
  → 教訓: この手の状態機械の変更は、既存の18曲だけでなく**多数シードの直接スキャン**
    （`TOREI._scheduleOnce` を100〜500回、脇本数・時間重複・リング参照の3不変条件を
    機械的にチェック）で検証しないと、稀な組み合わせでしか出ないバグを見落とす。
- **未対応（今後の課題）**: `preassign()` は和音の機会を意図的には作らない
  （たまたま手が空いていれば使われる、という受動的な発動）。「エチュード第2番」のように
  和音前提で作曲したい場合は、`make_midi.py` 側で「この演者はこの時刻に2音持たせる」を
  明示できる仕組みが別途要る（今回はスコープ外）。
  行動表テキスト（`TOREI.actionText`）とタイムライン描画は和音を明示する
  （タイムラインは「持っていた方」を中抜き丸、「新しく取った方」を塗りつぶし丸で上下に分けて表示）。

## 未着手・次の候補

- ~~保持キャッチ和音を前提にした新曲の作曲~~ → 2026-08-23 完了（測定器 `tools/analyze.mjs` を
  作り、それを見ながら「重ね鈴」を作曲。和音6/6成立）。`preassign()` が和音の機会を
  積極的に作らない点は**手つかずのまま**だが、作曲側の規則（持たれる側を4拍前に鳴らす）で
  実用上は狙って出せることが分かったので、強制指定の仕組みは今のところ不要と判断した。
- ~~メリーさんのひつじの稀な二重保持（seed 39/51/78）~~ → 2026-08-23 **これは実在しなかった**。
  検証ツール側のバグが見せていた幻影だった。詳細は「検証ツールの検証（メリーさんのひつじの
  『バグ』の顛末）」の節を参照。
- 実測値によるパラメータ較正（実際の持ち替え時間・投げの高さと滞空の関係）
- リングの回転アニメーション、パスの狙い先（隣だけに制限する等の距離モデル）
- GitHub Pages公開済み（https://aratama-ship-it.github.io/bell-toss/ 、2026-08-22）
- 狭い画面での二次操作（MIDI読み込み・書き出し・行動表をコピー）の畳み込み。
  DOM変更が要るため本人確認待ち（2026-08-23 の棚卸しで保留）

## UI補足（2026-08-21 時点）

- 演者名は行動表左の名前をクリックして編集（localStorage保存、舞台・警告・行動表に反映）
- 脇に挟んだリングは体側の細い縦楕円（横から見たリング）＋「脇」ラベルで表示

## 2026-08-25 Safari音ズレ対応（手動補正）＋バージョン表示常設

- **本人報告**: 音ズレはSafariのみで発生。Chromeとアプリ内ブラウザでは出ない。リロードでも直らない。
- **原因の見立て**: Safariは `AudioContext.outputLatency` を実装していない（undefined）。
  Chromeで効いている出力遅延の自動補正がSafariでは常に0になり、Bluetooth等の出力遅延
  （150〜300ms程度）が未補正のまま → 映像が音より先行する。コードのバグではなく
  「測れない環境」の問題なので、自動では直せない。
- **対応**: 設定行に「音ズレ補正」スライダー（0〜400ms、10ms刻み）を追加。tick の
  レイテンシ補正に加算して映像側を遅らせる。値は `localStorage("torei.avSync")` に保存。
  端末の性質なので**曲データ（songfile）には含めない**。目安: Safari+Bluetoothで150〜250ms。
- **バージョン表示**: h1 脇に `#app-version`（v2026.8.25-1）を常設。デプロイごとに手で更新し、
  報告にも毎回全バージョンを書く。**全プロジェクト共通ルールとしてメモリに保存済み**
  （feedback-version-display-and-deploy-report）。形式は日付ベース、同日2回目は -2。

## 2026-08-25 測定器の誤解を正す — 和音は「1手か否か」と「鳴るか否か」を分ける

`analyze.mjs` は同時刻2音が **1手の保持キャッチ和音** になった場合だけ ○ とし、それ以外を
一律 × と表示していた。しかし × の中には**両音とも普通にキャッチされていて和音として鳴っている**
ものが混ざる（複数の演者・手に分かれただけ）。「鳴らない」と「1手ではない」を同じ記号で
出していたため、ぶんぶんぶんが「和音2/3＝1つ鳴っていない」に見えていた。

- 実際: 10小節1拍 ミ+ド は 演者1(左)と演者2(左)が**同時にキャッチして両方鳴っている**。欠落ではない。
- 差は音楽的な質のみ。1手和音は一撃なので必ず揃う。分かれた場合は演者どうしの同期精度に依存する
  （わずかなフラムが出うる）。終止和音を2人で決めるのは画としてはむしろ映える。
- 修正: 3値表示にした。**○=1手の保持キャッチ和音 ／ △=鳴るが複数の手・演者に分かれる ／ ×=鳴らない。**
  サマリ列は `1手/鳴る/計`、合計行は「和音として鳴る/うち1手/鳴らない」。
- **全34曲の結果: 書かれた和音23箇所すべてが和音として鳴っている（うち1手和音22）。鳴らない0。**
- 教訓: 測定器の記号設計が雑だと、健全な状態を欠陥に見せて不要な作曲修正を誘発する。
  「達成できていない」と「理想形ではない」を同じ記号にしない。

## 2026-08-25 無駄な所作を減らす／開始時の持ち方を表示（v2026.8.25-2）

本人指摘3件への対応。

**① 「右手で投げ→左手でキャッチ→右手へ手渡し」の無駄**
原因は採点ではなく `bestCatch` のコスト。自分で投げて自分で受けるとき
**同じ手=0.25／逆の手=0** と、わざわざ逆の手へ渡る方を安くしていた。
`同0.2/逆0.22` に変更（投げた手で受けるのを既定にし、逆の手も僅差で残す）。
実測: 自分投げのうち逆の手が **127回→5回**、元の手へ戻す往復は **4回→0回**。

**② 開演時に演者1が脇へ挟むのは無意味**
準備配置が「1本目だけ手、残りは全部脇」の固定ルールだった（2026-08-24の修正が過剰）。
かといって全員「両手を先に埋める」にすると、曲頭で両手が塞がりパスを受けられない演者が出る
（2026-08-24に壊したのがこれ）。→ **演者ごとに seed から決める探索次元**にして、混在配置も試す。
準備で脇へ回す回数は全曲で **91→61回**。ぶんぶんぶんは 2→0（演者1は右手ミ／左手ソ）。

**③ 探索シード数 20→48**
探索次元が 3(avoidLevel)×2(passAggressive)×演者ごとの準備配置 に増えたのに20のままで、
良い解を取りこぼしていた。1曲あたり約80ms（従来36ms）。スライダーはデバウンス140msを追加。

**採点にも追加**: 持ち替え×0.5＋準備の脇×0.4（無駄な所作）と、パス率20%未満の急な減点
（本人指定の下限。比例ボーナスだけだと下限割れが選ばれうる）。
曲中の脇の出し入れは**減点しない**——3本以上を扱う演者には正当な技法で、
減点するとパス率を大きく削る（実測: グーチョキパー75%→31%）。

**結果（全34曲）**: パス率合計 +412pt（平均 +12pt）／持ち替え -20／準備脇 -30／
不可能0・振り0（**ぶんぶんぶんの振り1が消え、全曲が投げだけで成立**）／
和音は23箇所すべて鳴る（1手和音21）／両鳴り0・回復時間違反0・不変条件違反0。
20%未満はグリーンスリーブスのみ（16%。4人×10音高で構造的にパスが出にくい外れ値）。

**開始時の持ち方の表示**（本人要望）:
`TOREI.initialLayout()` を追加。ring.loc は曲を通して書き換わる最終状態なので使えず、
準備フェーズ（prep:true）の行動から開演時の配置を組み立てる。表示は3か所。
(a) 設定欄の下に色付きの丸＋音名で一覧、(b) Qシート冒頭に「開始時の持ち方」、
(c) 舞台の絵に音名ラベル（**停止中のみ**。再生中は飛球と重なって読めないので消す）。

**未決**: ぶんぶんぶん冒頭のソ・ファが自分投げになった（以前は ソ=パス）。
60BPMでは滞空1.0以下だと破綻なしのシードが48本中1本しかなく、選択の余地がない。
選択肢は次節の表のとおりで、リング数とのトレードオフ。本人判断待ち。

## 2026-08-25 二段構えへ（探索を分離し、配布する編成を固める）v2026.8.25-3

**発端と、私（AI）の誤りの訂正。**
本人から「これは全てシステマチックにやっているのか。曲ごとに先にしっかり最適化した方が
良いならその路線を取る」という問い。検証した結果、**指摘が正しく、私の従来の説明が誤りだった**。

私は「ぶんぶんぶんの冒頭を演者間パスにするのは60BPMでは構造的に不可能」「ミ+ドの終止和音は
成立しない」と説明していた。実際には**単に探索が足りていなかっただけ**。実測:

| 探索量 | 全34曲 平均パス | ぶんぶんぶん | 1曲あたり |
|---|---|---|---|
| 48シード（従来） | 82.7% | 45%・冒頭は自分投げ | 79ms |
| 200シード | 86.7% | **88%・冒頭3音すべてパス** | 307ms |
| 600 / 1500 | 87.7% / 88.3% | 同上（頭打ち） | 871ms / 2130ms |

ランダム再スタート法なので探索量がそのまま質になる。**「見つからない」と「存在しない」を
私が混同していた。** 以後、成立性を否定する前に探索量を上げて確かめること。

**構造。** ブラウザは編集のたびに再計算するので大きな探索量を置けない。役割を分けた。

- `tools/optimize.mjs` … 既定2000シードで曲ごとの最良を選び、`js/seeds.js` に seed を書く（全34曲76秒）
- ブラウザ … seed があればそれを1回再現するだけ（数ミリ秒）。無ければ通常探索
- 対話中の探索 … 上限200＋**十分に良ければ即打ち切り**（破綻なし・振りなし・和音成立・冒頭パス・
  パス率60%以上）。これで中央値15ms・最長138ms に収まる
- `TOREI.scoreResult()` … 採点をブラウザと最適化ツールで**共有**。別々に持つと必ず食い違い、
  「ツールが選んだ最良」と「画面に出る編成」がズレる

**固めることの本質は速さではなく再現性。** 配って稽古する振り付けが、こちらがスケジューラーに
手を入れるたびに黙って変わってはいけない。楽譜や設定を編集すると seed は自動で外れ（署名の
突き合わせ。編集の入口が多いので個々の操作に手を入れる方式は採らない）、**編集を元に戻すと復帰する**。
検証: 聖者の行進（seed700）で 65%→編集で63%→戻すと65%。

**採点に追加**: 冒頭の音が演者間パスでなければ +0.6（最も目を引く瞬間なので。2026-08-25 本人方針）。

**結果（全34曲・確定編成）**: 不可能0・振り0、和音23箇所すべて鳴る、両鳴り0・回復時間違反0・
不変条件違反0。パス率は大半が80〜100%。20%未満はグリーンスリーブスのみ（19%）。
ぶんぶんぶんは**パス88%・リング5本・冒頭3音すべて演者間パス**。

**AIに探索そのものをやらせない理由**（本人のMCP案への回答）: 物理制約の検査は
数百の時間区間の突き合わせで、LLMは「もっともらしいが物理的に不可能な」編成を自信を持って出す。
AIが向くのは「何を良しとするか」（演出方針・重み付け・仕上がりの判断）で、
成立性の判定と探索は決定論的なエンジンに任せる。MCPで繋ぐならその分担で。

## 2026-08-25 「無理のなさ」を重み付けできるようにした v2026.8.25-4

これまでの採点は「成立するか」「パスが多いか」だけで、**物理的に成立しても人に無理な編成**を
区別できなかった。本人要望を受け、稽古で効いてくる条件を測って採点に入れた。

**先に測った**（重みを付ける前に、全34曲でどの指標がばらつくかを確認。動かない指標に
重みを付けても意味がないため）:

| 指標 | 実測の幅 | 意味 |
|---|---|---|
| 窮屈な連続（同じ手が0.6秒未満で再び動く） | 3〜26回 | 忙しすぎ |
| 演者間の偏り（動作数の差） | 4〜53% | 一人に集中し他が手持ち無沙汰 |
| 高さの変化（同じ手の連投で段が変わる） | 0〜21回 | 高さが揃わず難しい |
| 遠パス（隣でない演者へ） | 3〜44回 | 距離が長く難度が上がる |
| 同時に空中のリング | 1〜3本 | （差が小さいので採用せず） |

**設計**: `TOREI.EFFORT` は「重み」ではなく**物理的に意味の分かる言葉**で持つ。
稽古で「手の余裕を0.8秒に」とは言えても「窮屈さの重みを0.15に」とは言えないため。
`TOREI.effortMetrics()` が実測し、`scoreResult()` が減点する。ブラウザ・最適化ツール・
点検ツールが同じ関数を見る。UIは `<details>` の折りたたみ（項目が多く常時展開は重いが、
隠すと存在に気づけないので、閉じていても何を設定できるか分かる見出しにする）。
**効き目は数字で出す**（窮屈◯回／偏り◯%）。見えないと調整のしようがない。

**既定値での効果（全34曲）**:

| | 窮屈 | 高さ変化 | 偏り平均 | 遠パス | 平均パス | リング計 |
|---|---|---|---|---|---|---|
| 重み無し | 429 | 87 | 26% | 396 | 88.8% | 258 |
| 重み有り | 344 | 54 | **16%** | 357 | 87.0% | 274 |

偏りが26%→16%と大きく改善。代償はリング +16本・平均パス -1.8pt。
リング本数も調整対象に含めた（`ringCost`。従来は0.1固定だった）。

**重みを変えると確定編成（seed）は自動で外れて再探索に切り替わり、戻すと復帰する**
（署名に `cfg.effort` を含めた）。配布用は `node tools/optimize.mjs` で焼き直す。

検証: 不可能0・振り0、和音23箇所すべて成立、両鳴り0・回復時間違反0・不変条件違反0。
